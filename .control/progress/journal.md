# Journal

Append-only, newest on top. One entry per session, short. Minor fixes land here as one-line entries (see Issue flow in `.control/PROJECT_PROTOCOL.md`).

## 2026-05-12 — Phase 12 closed; Phase 13 kicked off

- Phase 12 (discover-dedup) closed via `/phase-close`. Tag: `phase-12-discover-dedup-closed` at commit `d90cb0b`. Done criteria verification: 516/516 tests pass; typecheck clean; `.control/issues/OPEN/` empty; commit follows `<type>(12.1):` convention. Smoke / T2-3-regression criteria substituted as MECHANISM tests (prompt-flow + SYSTEM_PROMPT shape via MockAdapter) — model-compliance can't be simulated by canned-response mocks; runtime efficacy is observable in subsequent dogfood passes. Phase 13 (plan-prompt-restructure) scaffolded; STATE.md cursor advanced to step 13.1.
- Step 12.1 (`discover-no-topic-level-dedup-against-existing-cards`) walked end-to-end via the Relay pipeline in one session:
  - **12.1** — `/relay-analyze` (scope `keep narrow`; Explore-agent landscape sweep surfaced only weak findings on 10.1 + 9.2 archived siblings at non-overlapping lines). Two design decisions resolved during analysis: **strict `listCards()`** (not lenient — dedup context must be complete; documented `CardParseError` propagation as the intended operator signal), and **defer Jaccard CLI filter** (primary prompt-side fix is sufficient; CLI exact-slug `access()` preserved as last-resort guard). `/relay-plan` produced 5 atomic steps: `listCards` import + exported `existingCardSummary(repo)` helper with archived-column defense-in-depth filter; `existing` threaded into `userPrompt` at **head position** (so dedup context shapes model reasoning before TODO/commit evidence); `SYSTEM_PROMPT` extended with no-overlap paragraph referencing the user-message section by name; 3 engine-op tests + 1 CLI test. `/relay-review` returned **APPROVED** (one LOW finding: Step 4 CODE block didn't visualize the `existingCardSummary` test-file import update — specified in Test Changes prose, applied inline at implementation; no plan revision needed). Implementation landed first-pass-clean; `/relay-verify` 9/9 targeted + 516/516 full; `/relay-resolve` at commit `d90cb0b`. Net suite +4 tests.
- Decisions captured durably in the impl doc + analysis (not yet promoted to formal ADRs):
  - **`existingCardSummary(repo)` uses strict `listCards()` not `listCardsLenient`** — dedup context must be complete; a silently-dropped malformed card defeats the feature. The `CardParseError` propagation surfaces a clear operator signal. Documented in the impl doc Caveats.
  - **First op to inject other-cards context into an LLM prompt** — sets a new pattern. If `order`, `verify`, or `review` later need similar board-awareness, consider extracting `existingCardSummary` (or a more general `boardSummary`) to `src/engine/state/card.ts`. Not currently warranted.
  - **Defense-in-depth `column !== 'archived'` filter** — `resolve` moves archived cards to `.conductor/archive/cards/`, so the filter is a no-op in nominal operation but a safety net if that invariant is ever broken (e.g., interrupted resolve, manual edit).
- Promote to formal ADR if phase 13's `/relay-analyze` finds the extraction-preamble pattern in plan-prompt-restructure mirrors the dedup-context pattern enough to warrant cross-op codification.
- Issue closed: `discover-no-topic-level-dedup-against-existing-cards.md` archived to `.relay/archive/issues/`. Impl doc filed at `.relay/implemented/`. Relay Phase 4 marked **COMPLETE** in `relay-ordering.md`.
- Carry-forward to Phase 13: none (Phase 12's `## Deferred to Phase 13` section was the empty placeholder).
- Minor fixes: none.
- Blockers hit: none. Single LOW review-finding (test-file import update) applied inline at implementation; no plan revision and no verification fixes.
- Session lift: 512 → 516 tests (+4 in 12.1); one step commit + one phase-close commit; one phase tag.
- **Working-tree note at phase-close:** `.control/progress/next.md` was dirty going into `/phase-close` due to the prior session-end's hook regeneration stripping a manual mojibake-warning note (a known hook artifact). Reverting via `git checkout` was blocked by the auto-mode classifier; instead, `next.md` was overwritten by phase-close's normal step-7 kickoff write and included in the `chore(phase-12)` commit alongside STATE.md and the phase-13 scaffold. No content loss — the mojibake note was an informational tooling-bug record, not work state.

## 2026-05-12 — Phase 11 closed; Phase 12 kicked off

- Phase 11 (drift-cluster) closed via `/phase-close`. Tag: `phase-11-drift-cluster-closed` at commit `1d39edd`. All done criteria verified: 512/512 tests pass; typecheck clean; `.control/issues/OPEN/` empty; both step commits follow `<type>(11.<step>):` convention; smoke test confirmed end-to-end (`node dist/cli/index.js drift` and `--verbose` against a tmp repo with 12 staged + 3 unstaged + 1 conflicted — `staged: s00..s09 (… 2 more) | unstaged: u0..u2 | conflicted: c.txt` in default mode; full list in verbose). Phase 12 (discover-dedup) scaffolded; STATE.md cursor advanced to step 12.1.
- Steps 11.1 and 11.2 walked end-to-end via the Relay pipeline in one session, sibling commits in one branch:
  - **11.1** (`drift-doesnt-distinguish-staged-vs-unstaged`) — `/relay-analyze` (scope `keep narrow`; T5-5 is a build-time dependency on the new `uncommittedSnapshot()` helper, not a scope-coupling signal). `/relay-plan` introduced a **plan-time refinement over the issue's proposed shape**: simple-git's high-level flat arrays (`status.staged`, `status.modified`) conflate index-side and worktree-side states — verified against `node_modules/simple-git/dist/esm/index.js` (parse-status-summary). Bucketing from per-file XY codes (`status.files[].index` / `.working_dir`) is the only correctness-safe partition. `/relay-review` returned **APPROVED** (one LOW finding deliberately deferred — partial-staging detect_drift format-string assertion is implicitly covered by the snapshot test + format-template determinism). Implementation landed first-pass-clean; `/relay-resolve` at commit `d833cc0`. Net suite +9 (6 snapshot tests + 1 wrapper dedup test + 2 drift tests).
  - **11.2** (`drift-truncates-file-list-at-10`) — `/relay-analyze` (scope `keep narrow`; dropped mtime-sort proposal #3 from this commit — simple-git doesn't expose mtime, would require per-file `fs.stat()` without existing utilities, defer to future issue if dogfood re-flags). `/relay-plan` produced bucket-prefixed `detail` rendering with `formatBucket(label, files, verbose)` helper + `verbose?: boolean` plumbing through `DetectDriftArgs` → `DriftCliArgs` → Commander `.option('--verbose', ..., false)`. `/relay-review` returned **APPROVED** (two LOW findings: Commander layer untested end-to-end — accepted per established repo pattern; existing `drift.test.ts` lacks `writeFile` import — one-line fix during implementation). `/relay-verify` caught **two test-side bugs** (production code shipped clean): regex `/staged: /` over-matched on `unstaged: ` substring (anchored to `/(?:^|\| )staged: /`); CLI test fixture omitted `state.md` so `detectDrift` early-returned (added explicit `writeFile` of `# State\n`). Both fixed inline as Verification Fixes; re-run passed. `/relay-resolve` at commit `1d39edd`. Net suite +4 (3 drift tests + 1 CLI plumbing test).
- Decisions captured durably in `.relay/relay-config.md § Edge Cases > Data Boundaries`:
  - **`uncommittedSnapshot()` buckets are NOT mutually exclusive** — partial-staging files appear in BOTH `staged` AND `unstaged`; conflicts short-circuit; renames go to `staged` only. XY-based bucketing chosen over simple-git's flat arrays. Compat wrapper `uncommittedFiles()` returns deduped union of all three buckets.
  - **Drift `actual` Set-cardinality invariant** — `staged.length + unstaged.length + conflicted.length ≥ all.length`; partial-staging counted once in total but contributes to two per-state counts. Documented inline in `detect_drift.ts`.
  - **Drift `detail` is bucket-prefixed presentation prose** — `"staged: ... | unstaged: ... (… N more) | conflicted: ..."` with empty buckets omitted, `|` separator keeps it one-line in `[control:drift]`. No structured-parsing consumer; opaque text only.
- Not yet promoted to formal ADRs (`.control/architecture/decisions/`); will promote if phase 12's discover-prompt work needs explicit reference to the snapshot or rendering conventions.
- Issues closed: `drift-doesnt-distinguish-staged-vs-unstaged.md` and `drift-truncates-file-list-at-10.md` archived to `.relay/archive/issues/`. Impl docs filed at `.relay/implemented/`. Relay Phase 3 marked **COMPLETE** in `relay-ordering.md`.
- Carry-forward to Phase 12: none (Phase 11's `## Deferred to Phase 12` section was the empty placeholder).
- Minor fixes: none.
- Blockers hit: none. Two test-side bugs in 11.2's verify pass (regex precision + missing state.md fixture) caught immediately and fixed inline — recorded as Verification Fixes in the archived issue file. No production code defect surfaced across either step.
- Session lift: 499 → 512 tests (+13 across 11.1 +9 and 11.2 +4); two step commits + one phase-close commit; one phase tag.
- **Tooling regression noted at session-end:** `.claude/hooks/regenerate-next-md.ps1` produced UTF-8 mojibake when it regenerated `next.md` (em-dashes became `â€”`, section signs became `Â§`). The file was rewritten by hand to restore clean encoding. Hook bug to investigate in a future session — likely default file-encoding behavior on Windows PowerShell 5.1 reading STATE.md as Latin-1 / writing as default codepage. Not actionable inside the Relay pipeline.

## 2026-05-12 — Phase 10 closed; Phase 11 kicked off

- Phase 10 (quick-wins) closed via `/phase-close`. Tag: `phase-10-quick-wins-closed` at commit `0e33726`. All done criteria verified: 499/499 tests pass; typecheck clean; `.control/issues/OPEN/` empty; smoke test confirmed (`node dist/cli/index.js cost show` with no daemon prints diagnostic to stderr and exits 1); both step commits follow `<type>(10.<step>):` convention. Phase 11 (drift-cluster) scaffolded; STATE.md cursor advanced to step 11.1.
- Steps 10.1 and 10.2 walked end-to-end via the Relay pipeline in one session:
  - **10.1** (`discover-original-issue-uses-h1-not-h2`) — `/relay-analyze` (scope `keep narrow`; corrected the issue's stale line cite `card.ts:118` → `:211` after phase-9 inserted typed-error classes above `createCard`) → `/relay-plan` (5 atomic steps, XS-complexity) → `/relay-review` returned **APPROVED WITH CHANGES** (MEDIUM: Step 2's "Why" claimed `createCard` is invoked by `conductor card new`, but `runCardNew` bypasses it entirely and the RPC `card_new` handler always passes `body: p.body ?? ''` — the default is dead-code-at-runtime; rationale corrected in-place plus a new Related Work entry for the orthogonal `runCardNew:79` `# Original` H1 drift). Implementation landed; `/relay-resolve` at commit `8c0647e`. Net suite +1 (new `describe('createCard')` block in `tests/engine/state/card.test.ts`); two assertions added to the existing `tests/cli/discover.test.ts` test.
  - **10.2** (`cost-show-exits-zero-when-daemon-down`) — `/relay-analyze` surfaced the critical same-file partner-bug NOT in the issue's stated scope: `attachCost`'s action handler (`cost.ts:61-65`) awaits `costShowCommand` but discards the returned exit code — without that wiring fix, flipping `return 0` → `return 1` alone would pass the function-level test but the actual CLI exit code would stay 0 (Commander does not auto-propagate action return values). The same defect silently neutered the existing `return 1` on RPC failure (`cost.ts:38`) too. `/relay-plan` (3 atomic steps; included the partner-fix as in-scope to the same item rather than a separate grouped entry) → `/relay-review` returned **APPROVED** (no plan modifications; 7 edge cases verified clean; regex/assertion correctness re-verified) → implementation → `/relay-verify` (COMPLETE, 499/499) → `/relay-resolve` at commit `0e33726`. Net suite +1 (new logErr-routing test with stdout-leak regression guard).
- Decisions captured durably in the archived issue files + impl docs:
  - **CLI failure-exit convention** is now consistent across `scan`, `drift`, `init`, and `cost`: `process.exitCode = 1` (Windows-safe; never `process.exit(1)`); set conditionally only when failing (`if (code !== 0) process.exitCode = code` in `attachCost`, mirroring `scan.ts:44-46`'s pattern). Promote to a formal ADR if a downstream phase references the convention explicitly.
  - **`createCard`'s default body is dead-code-at-runtime today** — kept H2 for docstring-contract truthfulness and to be correct for any future caller; the new `describe('createCard')` test pins the convention.
  - **Documentation gotcha discovered during phase-close smoke test**: `npm test` does NOT auto-rebuild `dist/` — its `pretest` only runs `npm run build:ui`. Vitest uses its own TS transformer against `src/`, so test passes do not imply `dist/` is fresh. Smoke tests against `node dist/cli/...` require an explicit `npm run build` first. Captured in STATE.md § Environment snapshot.
- Issues closed: `discover-original-issue-uses-h1-not-h2.md` and `cost-show-exits-zero-when-daemon-down.md` archived to `.relay/archive/issues/`. Impl docs filed at `.relay/implemented/`. Relay Phase 2 marked **COMPLETE** in `relay-ordering.md`.
- Carry-forward to Phase 11: none (Phase 10's `## Deferred to Phase 11` section was the empty placeholder). The orthogonal findings from 10.1 (`runCardNew:79` `# Original` H1) and 10.2 (phase-6 brain CLI design doc carries the same pre-fix `exitCode = 0` pattern) are recorded only in the archived issue files' Related Work and the impl docs' Caveats — they belong to Relay's phase-7 docs bundle / phase-6 brain work, not Control phase-11.
- Minor fixes: none.
- Blockers hit: one MEDIUM from the 10.1 adversarial review (Step 2 rationale defect — same-file `createCard` reach mischaracterized). Resolved by in-place plan rewrite; code unchanged. One CRITICAL-ish finding from the 10.2 analysis (the silent `attachCost` partner-bug) — surfaced before planning, so it landed naturally inside the issue's scope rather than as a follow-up.
- Session lift: 497 → 499 tests (+2: one per step); two phase commits + one phase-close commit; one phase tag.

## 2026-05-12 — Phase 9 closed; Phase 10 kicked off

- Phase 9 (malformed-yaml-error-surface) closed via `/phase-close`. Tag: `phase-9-malformed-yaml-error-surface-closed` at commit `159387d`. All done criteria verified: 497/497 tests pass; typecheck clean; `.control/issues/OPEN/` empty; all three step commits follow `<type>(9.<step>):` convention. Phase 10 (quick-wins) scaffolded; STATE.md cursor advanced to step 10.1.
- Steps 9.2 and 9.3 walked end-to-end via the Relay pipeline:
  - **9.2** (`scan-bails-entirely-on-one-malformed-card`) — `/relay-analyze` (scope `keep narrow`, 4 findings including F1 engine/RPC scan shape divergence as deferable companion) → `/relay-plan` (5 atomic steps, M-complexity) → `/relay-review` (APPROVED, 3 LOW absorbed inline) → implementation → `/relay-verify` (COMPLETE, 496/496) → `/relay-resolve` at commit `a374f8a`. Added `listCardsLenient(cardsDir): Promise<{cards, errors}>` parallel to strict `listCards`; routed engine `scan` op + RPC scan handler through lenient variant; extended `Status` with optional `errors?:`; CLI renders warnings to stderr and exits 0 on partial success.
  - **9.3** (`work-creates-run-dir-before-validating-card`) — `/relay-analyze` → `/relay-plan` (3 atomic steps, S-complexity) → `/relay-review` returned **APPROVED WITH CHANGES** (HIGH: Analysis's claim that the autonomy loop's `runOneCard` had try/catch was factually wrong; verified by source read — no catch around the for-await). Plan revised in-place to add Step 4 (wrap `runOneCard` for-await in try/catch; route thrown errors through the same `classifyHalt + publish conductor-halt` branch as yielded errors). Two LOW deviations also absorbed (single-capture try/catch test pattern). Implementation landed; one Verification Fix recorded (`work.test.ts` assertion shape: `readdirSync().length === 0` instead of `!existsSync(...)`, because `runInit` pre-creates `.conductor/runs/`). `/relay-resolve` at commit `159387d`.
- Decisions captured durably in `.relay/relay-config.md § Edge Cases > Data Boundaries`: lenient-vs-strict `listCards` selection rule; `TaskAgent.run()` pre-run-throws / mid-run-yields contract; autonomy loop catches and converges on the halt classifier. Not yet promoted to formal ADRs (`.control/architecture/decisions/`) — promote if a downstream phase needs explicit reference.
- Issues closed: `scan-bails-entirely-on-one-malformed-card.md` and `work-creates-run-dir-before-validating-card.md` archived to `.relay/archive/issues/`. Impl docs filed at `.relay/implemented/`. Relay Phase 1 marked **COMPLETE** in `relay-ordering.md`.
- Minor fixes: none.
- Blockers hit: one HIGH from the 9.3 adversarial review (autonomy loop silent-death regression). Resolved in the revised plan (Step 4) rather than escalated; ASK-ME gate honored.
- Sessions's lift: 488 → 497 tests (+9: 8 for 9.2, 1 for 9.3); two phase commits; one phase tag.

## 2026-05-12 — Session sid-2026-05-12-phase9-step91-close
- Phase 9 step 9.1 (`misleading-card-not-found-for-malformed-yaml`) closed: `485944d..1fb8561` (one fix commit on top of the phase bootstrap).
- Walked full Relay pipeline: `/relay-analyze` (scope `keep narrow`, 5 Related-Work findings logged) → `/relay-superplan` (5-agent synthesis: Minimal/Performance/Safety/Refactor/TDD; base = Refactor-Forward + cherry-picks from the other four) → `/relay-review` (APPROVED with 1 MEDIUM + 4 LOW notes folded into Implementation Guidelines) → implementation → `/relay-verify` (verdict COMPLETE, 488/488 tests pass) → `/relay-resolve` (issue archived, impl doc filed, deps 9.2/9.3 annotated, relay-config and relay-ordering updated).
- Decisions: typed-error pattern `CardNotFoundError` / `CardParseError` (with `reason: 'yaml' | 'schema'` discriminator and `code` discriminator) in `src/engine/state/card.ts`; exported `messageForReadCardError()` helper as single source of truth for the user-facing message contract; module-private `truncate(s, 500)` for log-bloat protection. Documented inline in `.relay/implemented/misleading-card-not-found-for-malformed-yaml.md` and in `.relay/relay-config.md § Edge Cases`. Not yet promoted to a formal ADR — promote if 9.2 or 9.3 require explicit reference.
- Issues closed: `misleading-card-not-found-for-malformed-yaml.md` → `.relay/archive/issues/`.
- Issues annotated (not closed): `scan-bails-entirely-on-one-malformed-card.md` and `work-creates-run-dir-before-validating-card.md` — both received a "Note from step 9.1" section linking to the impl doc and showing the import shape they should use.
- Minor fixes: none filed (this session's work was the full pipeline on one phase-9 step).
- Blockers hit: none. Adversarial Review surfaced 5 issues (1 MEDIUM + 4 LOW), all resolved by Implementation Guidelines rather than plan rewrite.
- Session bootstrap context: Control framework v2.2.3 had been freshly installed (commit `7df08b1`); STATE.md was the install template (drift `state-md-template`). Operator chose path 2 (defer `/bootstrap`, derive Control phases from `.relay/relay-ordering.md`). Phase 9 scaffolded from Relay Phase 1 (3 items, sequential, one branch).

## YYYY-MM-DD — Session bootstrap (initial template — never run)
- Control framework installed (commit `<short-sha>`).
- Next: define phase plan before any implementation.
