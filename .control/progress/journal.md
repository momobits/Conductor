# Journal

Append-only, newest on top. One entry per session, short. Minor fixes land here as one-line entries (see Issue flow in `.control/PROJECT_PROTOCOL.md`).

## 2026-05-23 — Phase 29 closed (UI markdown render fix + unplanned brain step-resolver; 3 steps); Phase 30 kicked off (Frame B + dual-driver kickoff pending)

Phase 29 closed (tag: `phase-29-ui-markdown-render-fix-closed`, commit: pending phase-close); Phase 30 (Frame B and dual-driver; sequencing decision is the kickoff deliverable) kicked off. All Phase 29 done criteria verified including a Playwright smoke that exercised all three defensive markdown layers end-to-end (9/9 assertions pass).

- **Playwright smoke** (manual done-criterion this phase): seeded a temp smoke dir with a card body crafted to trigger all 3 defensive layers — mixed `\r\n` + `\n` line endings (layer 1), an unclosed `<details>` element followed by markdown that previously would have been swallowed (layer 2), standard markdown constructs that should never trigger the `<pre>` fallback (layer 3). Started a daemon against the smoke dir; navigated via the auth-token URL; ran a `browser_evaluate` assertion script that verified: 4 `<h2>` headings parsed across CRLF boundaries (L1 ✓); 0 actual `<details>` elements in DOM + literal `<details>` visible as escaped text (L2a/b ✓); Section 4 heading + `<strong>` + sub-H3 all render styled despite preceding unclosed tag (L2c ✓); no `<pre>` containing the full source (L3 ✓ — parse didn't throw); code fence + ordered list rendered; no literal `**bold**` or `## heading` text leaked into visible rendering. Screenshot captured (in `.playwright-mcp/`, gitignored); smoke dir + daemon + fixture script cleaned up. Working tree clean.
- **Carry-forward.** Phase 29's `## Deferred to Phase 30 (or later)` section contained only the literal `<item>` template placeholder. Per the runbook's carry-forward skip rule, no bullets seeded into Phase 30's "Why this phase exists" section. (Phase 28.3 had two real deferred items — `appendSection`/`extractSection` deprecation and UI artifact-panel layout polish — but those were Phase 28 deferred items, not Phase 29's, so they don't flow forward by protocol. Flagged in STATE.md's Notes section as context.)
- **Phase 30 scaffold** authored at `.control/phases/phase-30-frame-b-and-dual-driver/`. Step 30.1 is the kickoff sequencing decision: read 3 brainstorm files, weigh 3 sequencing options (Frame B first / dual-driver first / interleaved), document the decision in the README's "Why this phase exists" section, then author 30.2+ for the chosen first cohort. The directory name is intentionally neutral (acknowledges both clusters are in scope without pre-deciding ordering).
- **Issue filed against Control framework** (at `G:\Projects\Small-Projects\Control\issues\2026-05-23-regenerate-next-md-ps1-utf8-encoding.md`, not in this repo): `.claude/hooks/regenerate-next-md.ps1` mangles multi-byte UTF-8 characters when writing `next.md` on Windows PowerShell 5.1 (em dash → `â€"`, check mark → `âœ"`, etc.). Workaround: use the bash variant.
- **Phase 29 commits** (7 total): `c5807ba` fix(29.2) markdown render; `1cbdf8f` fix(29.3) brain step-resolver; `a3cd382` chore(install) relay-auto skill (this session, off-step); `c5983ce` docs(issues) dual-driver feature cluster (this session, off-step); `3848df7` docs(state) persist relay-auto session artifacts (this session, off-step); `f9af561` docs(29.3) backfill steps.md row (this session); `24a0cf0` docs(state) session end for step 29.3 (this session). Plus the upcoming `chore(phase-29)` close commit.

## 2026-05-23 — Phase 29 wrap-up (markdown render fix + unplanned brain step-resolver scope-add; 3 steps shipped); meta-tooling commits landed off-step; awaiting /phase-close

Phase 29 (`ui-markdown-render-fix`) shipped 3 steps end-to-end including one unplanned scope-add. STATE.md drift reconciled at session-end after the hook flagged a `commit-mismatch` at session-start (5 commits ahead of the recorded cursor). Working tree clean; awaiting `/phase-close` to tag `phase-29-ui-markdown-render-fix-closed`.

- **29.1** Analyze + bisect (`c5807ba` substituted for the originally-planned docs commit — work bundled into 29.2): `/relay-analyze` on `ui-markdown-render-breaks-partway-through-content.md` deferred the captured-repro bisect step (no dogfood repro available at analysis time) and identified three sibling root causes from symptom patterns: HTML-block pass-through on unclosed LLM-emitted `<details>`/`<div>`/`<table>`, mixed `\r\n`/`\n` line endings, no renderer error containment. Selected the **layered defensive normalization** approach that addresses all three simultaneously without needing a captured repro.
- **29.2** Markdown render fix (`c5807ba`): layered defensive fix in `src/ui/lib/markdown.ts` — line-ending normalization pre-pass + `marked.use()` renderer override for raw HTML escape + try/catch with `<pre>` fallback. Extracted pure helper to new `src/ui/lib/markdown_helpers.ts` (vendor imports in `markdown.ts` prevent direct testing — Implementation Deviation documented). Added 8 regression tests in new `tests/ui/markdown.test.ts`. Suite 764 → 772.
- **29.3** Brain step-resolver (`1cbdf8f`): UNPLANNED scope-add originally Phase 30 territory, pulled forward when issue #53 (`brain-cannot-advance-cards-past-approved-column`) surfaced mid-phase. Rationale: bug actively prevented dogfooding Phase 28's substrate work end-to-end (brain couldn't advance any card past `approved`). New `src/conductor/step_resolver.ts` parses H3 dotted-ID step headings from latest plan substrate, walks recent git log for `<type>(<phase>.<step>):` commit subjects scoped to the card's phase, returns discriminated `StepResolution = {kind:'resolved',step} | {kind:'no-plan'} | {kind:'unparseable-plan'} | {kind:'all-committed'}`. `defaultAgentFactory` wraps construction in async-generator IIFE to await the resolver; emits synthetic halts with case-specific reasons for non-resolved variants. `classifyHalt` adds `missing-step-arg` reason + pattern. Suite 772 → 784 (+12 net new). **Step shipped without a `phase-29/steps.md` row** — backfilled at session-end via `f9af561`. Process violation worth tracking: CLAUDE.md invariant requires the checkbox-flip in the same commit that closes the step; future unplanned-scope-adds MUST add the steps.md row inline.
- **Meta-tooling commits this session** (off-step but hook-legal): `a3cd382` chore(install) added the `relay-auto` skill in both `.agents/skills/` and `.claude/skills/` (drives Relay items through analyze → plan/superplan → review → implement → verify → resolve via isolated per-item agents, resumable on-disk state at `.relay/.auto-session/<timestamp>/`); `c5983ce` docs(issues) filed the 9-file `dual-driver-*` orchestration feature design cluster + brainstorm aggregator (new strategic cluster — sequencing vs Frame B not yet decided); `3848df7` docs(state) persisted three `.relay/.auto-session/` directories + refreshed `next.md`; `f9af561` docs(29.3) backfilled the missing steps.md row.
- **Drift reconciliation pattern.** The original commit attempt this session was `feat: add relay-auto skill ...` (no scope). The commit-msg hook rejected it. Operator authorized a 3-commit split: `chore(install)` for the skill, `docs(issues)` for the dual-driver cluster, `docs(state)` for the auto-session artifacts. Worth recording as an example of how to recover when meta-tooling work doesn't fit any active phase/step scope — the hook's allowlist includes `install`/`issues`/`state`/`spec`/`adr` precisely for these cases.
- **Pattern precedents advanced**: Pure-helper extraction n=15 → n=16 (Phase 29.2 `markdown_helpers.ts`). NEW: Discriminated-union return shape for resolve-or-halt outcomes (Phase 29.3) — n=1; promote at n=2. NEW: Layered defensive normalization for vendor-library output (Phase 29.2) — n=1; promote at n=2. JSONL/markdown-writer family unchanged at n=7 (no new artifact kinds this phase).
- **No formal ADRs filed.** ADR filing remains deferred per [[feedback-adr-scope-discipline]]. Strongest candidate (most overdue): JSONL/markdown-writer family at n=7.

## 2026-05-23 — Phase 28 closed (engine-ops body sunset; Frame B prerequisite; 3 sub-steps shipped end-to-end); Phase 29 kicked off

Phase 28 closed (tag: `phase-28-engine-ops-body-sunset-closed`, commit: pending phase-close); Phase 29 (UI markdown render fix; single P2 dogfood bug) kicked off. Single multi-day drive shipped all 3 Phase 28 sub-steps + manual smoke verified via hermetic TaskAgent harness + Playwright UI fetch verification.

- **Phase 28** (`engine-ops-body-sunset`) closed via `/phase-close`. Tag: `phase-28-engine-ops-body-sunset-closed`. Shipped the full engine-ops substrate migration completing what Phase 21 began for analyze/plan/chat:
  - **28.1** Review op + plan-op shim sunset (`8b2166d` + `11cab02`): new generic `findLatestArtifactRunId(repo, cardId, op)` helper in `run_artifact.ts` (filters `listRuns()` by canonical `<YYYYMMDDTHHMMSS>-<cardId>` shape via regex + length-equality guards; returns `{runId, text}` to collapse TOCTOU; treats empty/whitespace as no-artifact). `review.ts` migrated from `extractSection(card.body, 'Implementation Plan')` to substrate-read; from `appendSection(card.path, 'Adversarial Review', ...)` to substrate-write. `plan.ts` dual-write compat shim REMOVED. Prompt-shape decision (option a): keep card.body as `--- Card body (user description) ---` AND splice plan from substrate under `--- Implementation Plan (from substrate) ---` separator. 5 src + 9 test files; 11 cascading legacy-fixture failures discovered + fixed as Verification Fixes (Category A: 3 `toContain('## Implementation Plan')` flips; Category B: 5 bootstrap helpers seeding substrate plan runs). Suite 744 → 758 (+14).
  - **28.2** Verify + notebook ops (`97acffc` + `1ce2dd2`): mechanical pattern application reusing `findLatestArtifactRunId` for notebook→verify substrate read. Preserved notebook's `?? '_(none)_'` soft-fail fallback for cards without prior verify substrate (critical for Phase 2 work-test cascade prevention; 0 cascade confirmed). 5 src + 2 test files; +3 regression pins. Suite 758 → 761.
  - **28.3** Implement op + RPC + UI render widening (`fbb19de` + `e9c5c01`): final substrate migration + closes the 28.1↔28.3 RPC scope-seal. `ArtifactOp` writer-side union final shape `'analyze'|'plan'|'review'|'verify'|'notebook'|'implement'`; RPC enum at `schema.ts:117` widens to match atomically with `methods.test.ts:529-532`'s invalid-op swap (`'review'` → `'INVALID'`); UI `card_detail.ts` render typing widens via `ARTIFACT_OPS` Set + `isArtifactOp` type predicate (cleaner than the plan's `as` cast — adopted per adversarial review's optional refinement). **Bundled latent production prompt-bug fix**: pre-28.3 `implement.ts:94` spliced `card.body.trim()` under "Card body (Analysis + Plan)" label, but post-28.1+28.2 body had NEITHER section → near-empty prompt → garbage diffs in production (MockAdapter masked the bug in tests). 28.3 fixes by reading plan from substrate via `findLatestArtifactRunId`. Group-into-current-run per analysis rubric; kept the substrate migration atomic. `commitStep` `filesToCommit` drops `cardRelative` (body no longer mutated). `card.ts` header refreshed to "NO engine op accretes body sections via appendSection anymore". 6 src + 2 test files; +3 regression pins. Suite 761 → 764.
- **7 commits this phase** (6 in 28.x + 1 chore for smoke tooling): 3 `feat(28.<N>)` + 3 `docs(28.<N>) /relay-resolve` + 1 `chore(phase-28)` smoke harness. **Manual smoke verified 2026-05-23**: hermetic TaskAgent harness at `scripts/smoke-phase28.mjs` walked a fresh card discovered→archived (all 7 columns, all 6 ops; body byte-identity ✓; all 6 substrate artifacts present; src/smoke.ts written; git log has feat(smoke.1.1) commit). Playwright UI verification at `scripts/smoke-phase28-ui-fixture.mjs` seeded a fresh active card with all 6 artifacts; loaded Card Detail; verified via `browser_evaluate` that the RPC accepts all 6 op names + rejects invalid ops + the renderArtifact DOM build pipeline produces correct `<details open class="op-artifact op-<name>">` sections via marked → DOMPurify. Full-page screenshot captured (then cleaned up).
- **Critical RPC scope-seal pattern** established and proven across 3 sub-steps: writer-side `ArtifactOp` union widens incrementally; RPC enum + UI render typing stay narrow until the closing sub-step where they widen TOGETHER atomically with the boundary-guard test's invalid-op swap. This kept `methods.test.ts:529-532` green throughout the multi-step refactor without false-green windows. Worth promoting to ADR at n=2 (next multi-step RPC enum change).
- **Pattern precedent advanced**: JSONL/markdown-writer family is now at **n=7** (BrainLogWriter Phase 6 + RunLogWriter + ChatLogWriter Phase 21 + RunArtifactWriter Phase 21 writing analyze/plan + 4 new artifact kinds Phase 28). Well past the n=3 ADR-promotion threshold; remains operator-deferred per [[feedback-adr-scope-discipline]]. ADR slug candidate: `0003-jsonl-markdown-writer-pattern-family.md`.
- **Smoke harness scripts retained** as future tooling: `scripts/smoke-phase28.mjs` (hermetic TaskAgent lifecycle walker) + `scripts/smoke-phase28-ui-fixture.mjs` (active-card substrate fixture for Playwright UI verification). Useful for Frame B cohorts that will need similar UI-level smoke gates.
- **Frame B unblocked**: 6 designed feature files + 1 brainstorm aggregator at `.relay/features/` (card-detail-multi-surface-view, card-detail-op-controls-and-button-states, chat-driven-description-authoring, column-transition-op-triggering, brain-halt-on-user-chat, card-detail-run-history-surface) all declared `engine-ops-still-append-to-card-body` as their Prerequisite #0. Phase 28 closure satisfies the prerequisite. Frame B becomes the substantive Phase 30+ target after Phase 29 wraps the single remaining markdown-render bug.
- **Phase 29 scaffold** authored at `.control/phases/phase-29-ui-markdown-render-fix/`. 2 steps: 29.1 analyze + bisect, 29.2 implement + pin. Carry-forward from Phase 28's Deferred section was empty (template placeholder only), so no "Why this phase exists" seed.

## 2026-05-17 — Phase 27 closed (Relay Phase 15 brain telemetry cluster, 3 steps); Phase 28 kicked off

Phase 27 closed (tag: `phase-27-brain-telemetry-closed`, commit: `5f57f20`); Phase 28 (engine-ops body sunset; Frame B prerequisite) kicked off. Single drive shipped all 3 Phase 27 steps end-to-end + operator manual smoke verified all 3 visible behaviors against restarted daemon.

- **Phase 27** (`brain-telemetry`) closed via `/phase-close`. Tag: `phase-27-brain-telemetry-closed`. Shipped 3 brain-telemetry fixes closing Relay Phase 15 across **3 step closures in one session**:
  - **27.1** Stop button stopping-state + race (`0313db8` + `d90036e`): client-side optimistic flag (`stoppingBrain`) flips Stop button (disabled + "stopping…" text) and pill (amber "stopping · graceful drain" + pulsing dot) within ~10ms of click — before the RPC await yields. Defensive Start-button-disabled guard prevents click-Start-mid-stop races. Scenario A (drain-window no-feedback) fully resolved. Scenario B (tight self-halt race) partially resolved per issue's option #3 acceptance semantic: click-LANDS sub-case absorbed by optimistic UI; click-MISSES sub-case recoverable via brain-log row showing auto-halt reason. Phase 27 kickoff README authored in same docs commit. NEW `.brain-live[data-running="stopping"]` CSS variant uses `var(--amber)`.
  - **27.2** Verify-fail-then-wedge halt dedup (`076bd47` + `f30fc09`): operator-bound **Option C** (suppress meta-halt when previous iteration halted) over the issue's Option B preference. Threaded `halted: boolean` through `runOneCard` return shape; added `lastIterationHalted: boolean` Conductor field; conditional in wedge detector at `loop.ts:93-100` wraps `bus.publish + haltCount += 1` in `if (!this.lastIterationHalted) { ... }`. **Load-bearing `break;` stays outside the conditional** (loop always exits the wedge condition). Operator companion decision: do NOT increment haltCount on the suppressed path → `haltCount === number-of-published-halt-events` (internally consistent). Backward-compat preserved for escalation-wedge (no halt in iteration N → wedge detector still publishes). **Option B (new `conductor-wedge` event kind) deferred as Phase-28+ candidate** for distinct halt-vs-wedge semantics. +1 regression-pin test in `loop.test.ts` for the escalation-wedge backward-compat path.
  - **27.3** Brain-log accurate timestamps (`94b906e` + `e2524c7`): widened `brainLog: string[]` → `Array<{ts: number; line: string}>`; captured `Date.now()` at each of the 5 push sites (3 SSE handlers + 2 RPC-error handlers, including Phase 27.1's stop-error site); render derives per-row ts from `entry.ts` via `new Date(entry.ts).toLocaleTimeString(...)`. TypeScript-enforced push-site correctness — any missed site would fail typecheck. Server-side envelope-ts addition (issue's "even better" suggestion) deferred as Phase-28+ candidate; client-side `Date.now()` at SSE arrival is sub-millisecond-accurate over localhost.
- **6 commits this phase**: 3 `feat`/`fix(27.<N>)` + 3 `docs(27.<N>) /relay-resolve`. Suite 743 → 744 (+1 from 27.2's regression-pin test; 27.1 + 27.3 added zero new tests — file-internal closures, DOM-bound, vitest is node-env).
- **Playwright DOM verification became standard practice across all 3 steps** per the Phase 26.5b heuristic — caught the scenario B click-MISSES sub-case in 27.1 (resulting in the plan's Why-section split + new manual-smoke scenario in Post-Implementation Checks); confirmed re-paint stability in 27.3 (key behavioral guarantee that couldn't have been verified by static tests alone); confirmed visual rendering for both 27.1's amber stopping pill + 27.3's distinct row timestamps with attached screenshots.
- **Operator decision-time pick** worked cleanly for 27.2: 4-way analysis (A/B/C/D) with trade-off table surfaced via AskUserQuestion; operator picked Option C (recommended) + the haltCount-NOT-incrementing secondary detail; both bound inline in the analysis before /relay-plan ran. No re-work cycles.
- **Cumulative Monitor UX impact**: clicking Stop produces optimistic feedback within 10ms regardless of drain duration; brain-log shows 1 halt row per logical wedge (was 2); each row's timestamp reflects the actual event arrival time (was all identical to most recent paint time). Together these eliminate three distinct "this looks broken" moments operators were hitting during dogfood.
- **Manual smoke confirmed by operator** for all 3 visible behaviors after daemon restart. Phase 26.5b heuristic continued to pay dividends: operator's smoke flow was straightforward because the per-step Playwright verification during /relay-verify had already pinned the load-bearing behaviors.
- **Pattern precedents unchanged**: pure-helper-extraction stays at n=15; shared-module-for-cross-feature-consumption stays at n=4 (Phase 27 didn't extract new helpers or new shared modules; all 3 fixes were inline edits within existing functions). New `<verb>-ing…` button-text pattern noted at n=1 directly (Phase 23 routing UI is implicit n=0.5 precedent); promote at n=2.
- **Carry-forward (Phase 27 → Phase 28)**: Phase 27's `## Deferred to Phase 28 (or later)` section had only the `- <item> — <one-line reason for deferral>` template placeholder. Per the carry-forward rule, literal `<item>` placeholder is skipped — no carry-forward seeding into Phase 28's "Why this phase exists" section.
- **Phase 28 scaffolded**: single Relay item `engine-ops-still-append-to-card-body` (P2 L-complexity; the Frame B prerequisite per relay-ordering's strategic reframing) mapped to 3 Control steps (28.1 review op migration + plan-op compat shim sunset; 28.2 verify + notebook migration; 28.3 implement migration + UI artifact panel render-all-6 verify). Recommend `/relay-superplan` for 28.1 specifically given the L-complexity + cross-file plan→review dependency that must be cleanly orchestrated.

## 2026-05-17 — Phase 26 closed (Relay Phase 16 polish bundle, 5 steps + 1 corrective); Phase 27 kicked off

Phase 26 closed (tag: `phase-26-polish-bundle-closed`, commit: `27c687f`); Phase 27 (brain-telemetry) kicked off. Single session opened with `/session-start` against drift (benign self-reference commit-mismatch), drove all 5 Phase 26 steps end-to-end through the Relay pipeline, paused at `/phase-close` for operator manual smoke, surfaced a real regression on 26.5 via Playwright, shipped a corrective 26.5b additive fix, then re-ran `/phase-close` and tagged.

- **Phase 26** (`polish-bundle`) closed via `/phase-close`. Tag: `phase-26-polish-bundle-closed`. Shipped 5 polish-and-cosmetics fixes closing Relay Phase 16 + 1 dogfood follow-up across **5 step closures + 1 corrective in one session**:
  - **26.1** card-detail not-found empty shell (`4b87c9a` + `6d31c5d`): extracted `renderEmptyShell` helper to `src/ui/lib/empty_shell.ts` consumed by 4 sites (n=4 shared-module precedent); message-prefix detection after JSON-RPC serialization analysis revealed typed `code: 'CARD_NOT_FOUND'` is discarded at `src/daemon/http_server.ts:114`. +9 unit tests in `tests/ui/empty_shell.test.ts`.
  - **26.2** archived column FINAL badge (`f5afa98` + `dd72b56`): widened `policyBadge` + `policyForExit` types with `'final'`; new `.badge.final` CSS variant. Adversarial review caught width-fit concern (monospace + uppercase + 0.12em letter-spacing makes badge width scale linearly with char count); renamed `terminal` → `final` for visual rhythm.
  - **26.3** edition stamp ripped (`e38ca94` + `24b7b8a`): operator-bound Option A (rip) after analysis surfaced that issue's proposed Option B (read from `engine_state` RPC) was overly optimistic — no such RPC exists, would push from XS to S/M. Three viable variants presented; operator chose minimum-scope cleanliness.
  - **26.4** favicon SVG (`d49ef67` + `d050fd2`): 16x16 `§`-on-`--ink-500` SVG, `<link rel="icon">` in `<head>`, build-ui predicate extended to copy `.svg` files. Static handler's MIME table already supported `image/svg+xml`.
  - **26.5** LIVE FEED label clipping — **two-pass**: original fix (`ec2a7ba` + `3012643`) added `margin-bottom: 18px` to `#work-btn`, which solved a non-bug (the label was never clipped by the button). Playwright smoke during phase-close revealed actual cause: `.stream { overflow-y: auto }` clipping its own `::before` pseudo at `top: -8px`. Corrective `fix(26.5b)` (`5fc4395`) split visual frame from scroll container — outer `.stream` keeps the `::before` label, inner `.stream-scroll` holds `max-height + overflow-y`. The `id="stream"` moved to the inner div so existing `streamEl = root.querySelector('#stream')` reference + all `appendChild` / `scrollTop` calls continued to work unchanged.
- **12 commits total**: 4 `feat(26.<N>)` + 1 `fix(26.5)` + 1 `fix(26.5b)` + 5 `docs(26.<N>)` /relay-resolve close-outs + 1 `chore(phase-26): close phase 26, kick off phase 27`. Suite 734 → 743 (+9 from `tests/ui/empty_shell.test.ts` at 26.1; steps 26.2 / 26.3 / 26.4 / 26.5 / 26.5b added zero new tests — file-local helpers + visual-only CSS changes; verified via manual smoke + Playwright).
- **Pattern precedents advanced**: pure-helper-extraction now at **n=15** (Phase 26.1 added `renderEmptyShell` + `escapeHtml`); shared-module-for-cross-feature-consumption now at **n=4** (Phase 26.1 added `src/ui/lib/empty_shell.ts` consumed by 4 callers). Both well past the n=2+ ADR threshold; filing remains deferred per the 2026-05-15 operator decision.
- **New heuristic captured 2026-05-17** in `.relay/implemented/ui-stream-live-feed-label-clipped-by-work-button.md` § Verification Fix (26.5b): future XS visual-fix analyses must explicitly check parent-overflow as a candidate cause when an absolutely-positioned descendant is being cropped. The Phase 26.5 first-pass missed this — assumed the LIVE FEED label was clipped by the work-button's painted region above; Playwright runtime toggle of `overflow: visible` proved the actual mechanism. Heuristic candidate for promotion to ADR if a second visual-fix analysis would have benefited from this rule.
- **Operator decision (26.3)**: surfaced when analysis revealed the issue's proposed direction had a wrong premise (no `engine_state` RPC exists). Three within-XS-scope variants offered (rip, date-derived, package-version); operator chose Option A (rip) for minimum-scope cleanliness. Documented as "Operator-bound approach" in the issue's archive entry.
- **Operator pause at `/phase-close`**: round-1 close-out paused for manual smoke verification before tagging. Operator's smoke caught the real 26.5 regression that all unit tests + typecheck + build had passed cleanly. This is the protocol working as designed — automated coverage CAN'T catch overflow-clipping of an absolutely-positioned descendant; human-in-the-loop visual smoke is the gate.
- **Carry-forward (Phase 26 → Phase 27)**: Phase 26's `## Deferred to Phase 27 (or later)` section had only the `- <none yet>` placeholder. Per runbook, lacking the ` — ` em-dash separator → non-conforming bullet → seeding skipped. Phase 27's `## Why this phase exists` keeps its `<Fill in during phase kickoff.>` placeholder pending Phase 27 kickoff authorship.
- **Phase 27 scaffolded**: 3 steps mapping 1:1 to Relay Phase 15 brain-telemetry cluster (#31 P2 S Stop button stopping-state + race, #32 P3 S halt-event deduplication, #33 P3 XS brain-log timestamps from event-ts not paint-time). All three touch `src/ui/views/monitor.ts` + `src/conductor/loop.ts`; bundle as one PR. Top item: `/relay-analyze ui-monitor-stop-button-no-stopping-state-and-tight-race-window.md`.
- **All 5 visual smokes verified via Playwright** against running daemon at phase-close gate (round 2 after the 26.5b corrective fix). Smoke evidence captured under `.playwright-mcp/` (gitignored).
- **Heads-up for Phase 27**: known parallel-runner flake on `tests/conductor/loop.test.ts > Daemon shutdown stops the conductor brain` touches the same surface Phase 27 modifies (`src/conductor/loop.ts`, especially #32 halt coalescing). Watch closely — may incidentally resolve OR may surface as a real regression.

## 2026-05-16 — Phase 25 closed (Relay Phase 17 keyboard layer, 5 steps); Phase 26 kicked off

- **Phase 25** (`keyboard-layer`) closed via `/phase-close`. Tag: `phase-25-keyboard-layer-closed`. Closed the entire Relay Phase 17 keyboard layer across **5 steps in one session**: 25.1 dispatcher → 25.2 board nav + move chord → 25.3 shared approval dialog (grouped with Phase 16 #35) → 25.4 footer rotation + help overlay (grouped with migrated R-key issue) → 25.5 smoke-surfaced ergonomics revision (QWERTYU column keys + A refresh).
- **9 commits**: 4 `feat(25.<N>)` + 4 `docs(25.<N>) /relay-resolve` + 1 final `feat(25.5)`. The 25.5 commit was single-shot (no separate docs close-out) since it's a focused UX patch surfaced during operator manual smoke before tagging. Suite 666 → 734 (+68 across the phase; 5 new/extended test files).
- **Two grouped runs (25.3 + 25.4)** each closed a sibling issue alongside the run leader (Phase 16 #35 and the migrated R-key issue, respectively). Parent brainstorm `ui-keyboard-accessible-board-transitions.md` transitioned to COMPLETE and archived alongside 25.4 per the relay-resolve workflow's "all features resolved" branch. Relay Phase 17 fully closed.
- **One /relay-superplan (25.2)** dispatched 5 parallel Plan-agents (Minimal Change, Performance-First, Safety-First, Refactor-Forward, Test-Driven). Performance-First was the only agent to catch a load-bearing correctness defect: the column-pulse CSS animation needed to target `.column::before` (the visible `01..07` numeral rendered via `content: attr(data-num)`), not `[data-num]` as a child selector (which matches nothing). Adopted in the synthesized plan.
- **One CRITICAL hole caught by adversarial review (25.2)**: `lib/keys.ts:57-67` fires `1/2/3 → navigateTo` BEFORE delegating to `boardKeyHandler`. In move-mode, that would have blocked `discovered→planned` (`2`), `planned→approved` (`3`), and 3 backward edges — the kanban-gateway transitions. Fix: extend `KeyContext` with `boardInMoveMode: () => boolean`; gate the view-switch on `!ctx.boardInMoveMode()`. The 25.5 ergonomics revision later made this gate structurally inert (columns are letters now), but kept defensive.
- **Smoke-surfaced UX revision (25.5)**: operator caught during the post-25.4 smoke that `1/2/3` are also Board column hotkeys, colliding with view-switch. Remapped Board columns to `Q W E R T Y U` (QWERTY top-row left half) and refresh from `R` to `A`. Column header labels in the UI also switched from `01..07` to `Q..U` (`.column::before` via `attr(data-num)`). Bundled in-phase as step 25.5 rather than filed as a follow-up; suite landed at +5 with regression pins on the old keys.
- **Manual smoke confirmed by operator** for all 5 steps before tagging.
- **No verification fixes** beyond the one Step-2-during-25.2 noMods refinement (move-mode "any other key exits" gated on `noMods` so Ctrl+1 doesn't accidentally exit move mode); documented in 25.2's spec under Implementation Deviations.
- **Pattern precedents advanced**: pure-helper extraction reached **n=14** (added `isInFormField`, `handleKey`, `decideBoardAction`, `resolveArrowAcross`, `selectBody`, `selectFooterShortcuts`, `formatFooterHtml`). Shared-module-for-cross-feature-consumption reached **n=3** (board_validate.ts, lib/dialog.ts consumed by 3 callers, lib/footer.ts consumed by 2). Both well past the n=2+ ADR threshold; filing remains deferred per operator decision.
- **Issues closed**: 2 — Phase 16 #35 (`ui-transition-dialog-references-internal-phase-terminology`, via 25.3 grouped run) + migrated R-key issue (`ui-footer-r-key-affordance-not-wired`, via 25.4 grouped run). **Brainstorm archived**: 1 — `ui-keyboard-accessible-board-transitions.md` (all 4 features resolved).
- **Phase 26 kicked off**: target Relay Phase 16 polish bundle (4 XS items remaining: #34 deeplink-not-found, #36 archived-badge, #37 edition-stamp, #38 favicon). One bundled PR per the relay-ordering recommendation.

## 2026-05-16 — Phase 24 closed (Relay Phase 14 grouped run); Phase 25 kicked off

- **Phase 24** (`board-transition-ux`) closed via `/phase-close`. Tag: `phase-24-board-transition-ux-closed`. Closed Relay Phase 14 as a grouped run of 2 entries: #29 leader (board drag-drop offers approval for invalid transitions; blocking `alert()` on server reject — P2, S-complexity) + #30 sibling (no backward UI path out of `approved` — P2, XS-complexity). Both full closures. Four commits: `e86b63e feat(24.1)` (new `src/ui/views/board_validate.ts` pure-helper module with `FORWARD_MAP`, `nextColumn`, bidirectional `isLegalTransition`; 54-entry parity test against engine `canTransition`) + `0726b94 feat(24.1)` (lifecycle `BACKWARD` set adds `'approved->planned'`) + `16446ff feat(24.1)` (board_dnd pre-validates client-side; shake on illegal; `alert()` → `console.warn`; board.ts policyForExit uses shared nextColumn; app.css `.shake` rule) + `098d338 docs(24.1)` (/relay-resolve close-out). Suite 612 → 666 (+54 vitest entries, including a 49-pair parity-with-canTransition `it.each`).
- **Substrate delivery**: `src/ui/views/board_validate.ts` matches the public API Phase 17 #41 (`keyboard-board-focus-and-move`) was already designed against. Phase 17 step 25.2 (in the upcoming Phase 25) imports it directly — substrate-first delivery means Phase 25's keyboard work doesn't redo the extract.
- **One adversarial review APPROVED** (clean) — no plan changes required. Two LOW-severity items acknowledged: `.card-tile.shake` keyframes interact with `:hover` transform via CSS precedence (animation wins during 220ms playback; hover restores on completion — no fix needed); Phase 17 #41 narrative-vs-symbol-name divergence (says "forward map" but symbol `isLegalTransition` is already bidirectional-friendly — Phase 17's eventual /relay-analyze will reconcile its narrative).
- **No verification fixes** — implementation matched the plan exactly on first pass. One pre-existing parallel-runner flake surfaced during /relay-verify (`tests/conductor/loop.test.ts > Daemon shutdown stops the conductor brain` timed out at 5000ms under load; passed in isolation at 810ms; touches no Phase 24 surface; not a regression). Tracking as known flake; documented in STATE.md and the impl doc's Caveats.
- **Pattern precedents advanced**: pure-helper extraction reached **n=7** with `nextColumn` and `isLegalTransition`. NEW pattern variant flagged: **"shared validator module extracted for cross-feature consumption"** (n=1) — `board_validate.ts` is the first instance designed explicitly to serve a not-yet-built downstream feature. Watch for repetition through Phase 25-26; if a third site adopts, warrants its own ADR.
- **Manual smoke** confirmed by operator: drag-drop illegal transitions shake the source tile silently (no dialog, no alert); `approved → planned` rollback works through UI dialog and CLI; both surfaces in agreement.
- **Issues closed**: 2 — `ui-board-dnd-invalid-transition-uses-server-error-alert` (Phase 24 leader, full) + `ui-no-backward-path-from-approved-column` (Phase 24 sibling, full). **Issues filed**: 0.
- **Phase 25 kicked off**: target Relay Phase 17 (keyboard layer, 4 designed features in strict declared order: #40 dispatcher → #41 board keys → #42 dialog bindings → #43 footer + overlay). 4 steps in Phase 25's steps.md, mapping 1:1 to features #40-#43.

## 2026-05-16 — Phase 23 closed (Relay Phase 13 PR-2 grouped run); Phase 24 kicked off

- **Phase 23** (`routing-pr2`) closed via `/phase-close`. Tag: `phase-23-routing-pr2-closed`. Closed Relay Phase 13 PR-2 as a grouped run of 2 entries: #24 leader (autonomy dropdown overwrites uncommitted yaml edits — P1, S-complexity) + #27 sibling (config_set strips yaml comments — P2, M-complexity). Both full closures. Four commits: `ce704fa feat(23.1)` (autonomy dropdown surgical patch via `replaceAutonomyDefault` pure helper) + `552d1e2 feat(23.1)` (`preserveYamlComments` pure helper — file-head preamble + section-leading blocks + EOL annotations via unified KV_PATTERN) + `48ace63 feat(23.1)` (wire helper into `config_set` RPC + `autonomy.ts` CLI sibling write site) + `e0295a8 docs(23.1)` (/relay-resolve close-out). Suite 596 → 612 (+16: 6 unit + 8 unit + 1 integration + 1 CLI). Relay Phase 13 cluster (PR-1 + PR-2) fully resolved across Control Phases 22 + 23.
- **One /relay-review APPROVED-WITH-CHANGES verdict** during this session — caught two LOW-severity defensive fixes pre-implementation:
  - **Issue 1**: `replaceAutonomyDefault` used `$1${mode}` replacement string. `mode` is bounded to four enum values (no `$`), but defensive function-replacer style (`(_, p1) => p1 + mode`) avoids any future regex-replacement-special interpretation if `AUTONOMY_MODES` ever expands.
  - **Issue 2**: `preserveYamlComments` Pass 3 split TOP_KEY vs NESTED_KV regexes, capturing EOL comments only from nested keys. Top-level scalars (`verify_command: x  # custom`) lost their annotations. Unified into a single `KV_PATTERN` that captures EOL for both top-level and nested; added an 8th unit case for top-level scalar EOL preservation. Net result: suite delta updated from +15 → +16.
  - Review followed the ASK-ME gate (paused for operator confirmation before implementation). The plan was approved on the operator's "go".
- **One Verification Fix** during /relay-verify: `tests/config/preserve_comments.test.ts > drops comments that no longer have a matching key in the dump` failed because the test input placed `# orphan section` at file-head (which the helper correctly preserves as preamble). The test's intent was the inter-key orphan case — fixed by restructuring the input so the comment sits between two top-level keys, with the second key absent from the dump. Test-only revision; helper logic unchanged. Documented in the spec's `## Verification Fixes` section per the /clear instructions (apply inline; no operator pause).
- **Sibling-bug closed in the same run**: the /relay-analyze related-work scan flagged `src/cli/commands/autonomy.ts` as a candidate site for #27 (same `yamlDump`-after-parse pattern). /relay-plan verification confirmed YES and folded it into Step 5 of the plan; the CLI command got its first dedicated test (`tests/cli/autonomy.test.ts`) and now uses the same `preserveYamlComments` helper as the RPC `config_set` path.
- **Pattern precedent advanced**: pure-helper extraction reached **n=6** with `replaceAutonomyDefault` (n=5) and `preserveYamlComments` (n=6). Promotion threshold long fired. ADR filing remains deferred per operator decision; candidate slug `0001-pure-helper-extraction-for-testable-cli-contracts.md` (verify next number against `.control/architecture/decisions/`).
- **Issues closed**: 2 — `ui-routing-autonomy-dropdown-overwrites-uncommitted-yaml-edits` (Phase 23 leader, full) + `ui-config-set-strips-yaml-comments` (Phase 23 sibling, full). **Issues filed**: 0.
- **Phase 24 kicked off**: target Relay Phase 14 (#29 board drag-drop validator + alert removal; #30 backward path approved→planned). Phase 24's deliverable extracts the shared `src/ui/views/board_validate.ts` validator that Relay Phase 17 #41 (`keyboard-board-focus-and-move`) will later import — this phase unblocks the keyboard cluster mechanically.

## 2026-05-16 — Phases 21 + 22 closed (back-to-back grouped runs); Phase 23 kicked off

- **Phase 21** (`card-body-persistence`) closed via `/phase-close`. Tag: `phase-21-card-body-persistence-closed` at `098474c`. Closed Relay Phase 12 as a grouped run of 4 entries: #20 leader + #21 + #22 + #23. Three full closures (#21 extractSection regex eliminated via in-memory `PlanArgs.analysis`; #22 chat JSONL substrate + UI replay + legacy `## Chat` body strip; #23 assistant turns through `renderMarkdown`). One documented partial closure (#20 plan body dual-write shim retained for deferred review op; body bloat reduced ~114 → ~50 lines/click). Commits: `b81bcd6 feat(21.1)` (run-artifact substrate) + `8cc3bad feat(21.1)` (chat sibling + UI replay) + `3f46351 feat(21.1)` (assistant markdown) + `c7579d9 docs(21.1)` (checkbox) + `4b4c270 docs(21.1)` (/relay-resolve). Suite 559 → 585 (+26).
- **Phase 22** (`routing-config-destructiveness`) closed via `/phase-close`. Tag: `phase-22-routing-config-destructiveness-closed` at `e46d52e`. Closed Relay Phase 13 PR-1 as a grouped run of 3 entries: #25 leader + #26 + #28. All full closures. Commits: `c22cb0c feat(22.1)` (cost_ceilings preprocess null→Infinity) + `9053529 feat(22.1)` (server-side deep-merge in config_set, **bypassing ConfigSetParams.parse** per critical /relay-review fix) + `cc86027 feat(22.1)` (ZodError joined message + structured `error.data.issues`) + `9c2a8f6 docs(22.1)` (checkbox) + `789c460 docs(22.1)` (/relay-resolve). Suite 585 → 596 (+11). PR-2 (#24 + #27) unblocked for Phase 23.
- **Two /relay-review APPROVED-WITH-CHANGES verdicts** during this session — both caught load-bearing pre-implementation bugs:
  - **Phase 21**: original Step 3 removed plan's `appendSection(card.path, 'Implementation Plan', resp.text)` entirely. Adversarial review surfaced that `src/engine/ops/review.ts:41` reads `## Implementation Plan` from card body via `extractSection` and throws if missing — every card transitioning past `planned` would have broken. Fix: dual-write compat shim retained; sunset filed as Phase 22+ follow-up issue `engine-ops-still-append-to-card-body.md`. Also: HIGH greedy-regex in body-strip caught (mid-body `## Chat` would lose later sections); fixed with non-greedy + lookahead.
  - **Phase 22**: original Step 2 called `ConfigSetParams.parse(raw)` before the merge. Adversarial review (with empirical zod 3.23.8 verification: `z.object({a: z.string(), b: z.string().default('B')}).parse({a:'x'})` returns `{a:'x', b:'B'}`) surfaced that the pre-parse FILLS defaults for omitted top-level fields — the subsequent merge would then overlay those defaults onto disk, defeating the purpose entirely. Fix: bypass `ConfigSetParams.parse`; shape-check via `z.object({ config: z.record(z.unknown()) })`; treat inner config as `Record<string, unknown>` partial; validate the MERGED result via strict schema. Plus MEDIUM ENOENT-guard for missing config.yaml race.
  - Both reviews followed the ASK-ME gate (paused for operator confirmation before implementation). Without them, both phases would have shipped broken: Phase 21 would have broken the entire `planned → approved` workflow; Phase 22 wouldn't have actually fixed #25.
- **Patterns reached new n-counts**: pure-helper extraction at **n=3+** (Phase 18 `formatDaemonStartedMessage` + Phase 20 `detectPythonVerifyCommand` + Phase 21 substrate helpers + Phase 22 `deepMergeConfig`/`isPlainObject`). JSONL/markdown-writer family at **n=3** (Phase 21 `RunArtifactWriter` + `ChatLogWriter` joined `RunLogWriter` + `BrainLogWriter` family). ADR filing remains deferred per operator decision; no new pressure introduced.
- **Bootstrap surfaced material drift**: STATE.md at session start claimed "active Relay backlog is empty" but the working tree contained a 2026-05-15 Playwright dogfood result (20 issues + 4 designed feature files) that hadn't been committed. Reconciled with the operator-driven housekeeping commit `807f475 chore(phase-21): kickoff card-body persistence — adopt 2026-05-15 dogfood backlog` which adopted the dogfood items wholesale and added `@playwright/test ^1.60.0`. `.gitignore` extended for `.playwright-mcp/` cache + root-level dogfood screenshots.
- **Issues closed**: 7 total — `ui-work-card-output-persisted-into-card-body` (Phase 21 leader, partial) + `ui-plan-op-cannot-see-analyze-output-it-just-wrote` + `ui-chat-history-not-loaded-on-revisit-but-pollutes-card-body` + `ui-card-chat-renders-markdown-as-plaintext` (Phase 21 siblings, all full) + `ui-routing-yaml-commit-silently-resets-omitted-fields-to-defaults` (Phase 22 leader, full) + `ui-config-get-set-roundtrip-fails-on-infinity-serialization` + `ui-routing-save-error-renders-raw-zod-json` (Phase 22 siblings, full). **Issues filed**: 1 — `engine-ops-still-append-to-card-body.md` (Phase 22+ follow-up; obligation includes plan body dual-write sunset).
- **Phase 23** (`routing-pr2`) scaffolded; STATE.md cursor advanced to 23.1; carry-forward bullet from Phase 22's Deferred section seeded into Phase 23's "Why this phase exists" (Relay #24 + #27 PR-2 cluster).
- Minor fixes: legacy test assertions in `tests/cli/work.test.ts` + `tests/integration/end-to-end.test.ts` (Phase 21 contract migration), `tests/rpc/methods.test.ts` chat-test rewrite (Phase 21 Commit B).
- Session lift: 559 → 596 tests (+37); 13 commits across two phase cycles + 2 phase tags.

## 2026-05-15 — Phase 20 closed; Phase 21 kicked off (TBD scaffold; Relay backlog empty)

- Phase 20 (`init-verify-venv-awareness`) closed via `/phase-close`. Tag: `phase-20-init-verify-venv-awareness-closed` (created during this `/phase-close`; placed at HEAD before the phase-close commit). All done criteria verified: `4f5ac48 feat(20.1)` ships step 20.1 with checkbox flip in `654973f docs(20.1)` (CLAUDE.md "flip in same commit" invariant was missed initially; surfaced + fixed during `/relay-resolve` walk-out); `.control/issues/OPEN/` empty; `npm test` 559/559 across 98 files in ~16.9s; targeted `tests/cli/init.test.ts` 32/32 (+15 from baseline 17); `npm run typecheck` clean; working tree clean; all 20.x commits follow `<type>(20.<step>):` convention.
- Step 20.1 walked the full Relay pipeline end-to-end: `/relay-analyze` (Agent(Explore) for landscape scan; main-session read of 5 affected source files) → `/relay-plan` (single-pass, M complexity) → `/relay-review` (**APPROVED WITH CHANGES** — 2 MEDIUM + 2 LOW applied in-place) → implement → `/relay-verify` (verdict COMPLETE on first pass, no verification-fix iterations) → `/relay-resolve` (impl doc filed; source issue archived with banner; relay-ordering Phase 11 marked COMPLETE; commit `feat(20.1): detectVerifyCommand venv-aware for Python (uv/pdm/poetry/.venv/venv/python -m pytest)`).
- Adversarial review's 2 MEDIUM findings caught genuine pre-implementation bugs: **Issue 1** — `node:path.join` is host-platform-dependent at runtime, undoing the helper's `platform` parameter; fix: separate host-aware `access()` (via `join`) from target-aware command-string composition (explicit `sep` derived from `platform` arg). **Issue 2** — markers-array reorder accidentally changed Makefile-vs-pyproject precedence, violating the issue body's L91-95 deferral of that nit; fix: preserve array order, delegate to helper inline within the loop. Both fixed in the plan in-place before code was written. Without `/relay-review`, the platform bug would have caused either win32-or-posix CI failures (run-platform-dependent which branch fails).
- **`--browser` flag reclassified WONT-DO** earlier in the session (during Phase 19 close, before Phase 20 work began): operator-driven scope decision. Banner `> **ARCHIVED — WONT-DO**` applied to `.relay/archive/issues/daemon-start-missing-browser-flag.md`; relay-status.md and relay-ordering.md reconciled; memory saved (`memory/feedback-archive-folder-is-intentional.md` + `memory/project-browser-flag-wontdo.md`) so future sessions read the archive-folder signal correctly. Earlier in the session I had also flagged this as "misplaced" per `/relay-scan`'s warning wording; operator correction taught me archive-folder placement is intentional WONT-DO.
- **Pure-helper-extraction pattern reached n=2** with this work (Phase 18 `formatDaemonStartedMessage` = n=1; Phase 20 `detectPythonVerifyCommand` = n=2). STATE.md's promotion criterion for the pattern fired. Operator chose to defer ADR filing rather than expand Phase 20's scope (memory saved at `memory/feedback-adr-scope-discipline.md` so future-me doesn't auto-bundle ADR filing into implementation work-items). Pattern + n-count recorded durably in impl doc Caveats and in Phase 20 README's "ADRs decided in this phase" section. ADR slug candidate `0001-pure-helper-extraction-for-testable-cli-contracts.md` for a future session.
- Phase 21 (`tbd`) scaffolded as a bare placeholder; STATE.md cursor advanced to 21.1 (TBD); no carry-forward (Phase 20's Deferred section was the template placeholder). Active Relay backlog is now **empty** — all 20 items closed across 11 Relay phases (the 2026-05-12 dogfood backlog's 17 items + Phase 9 gitignore-template carry-over + 2026-05-15 omniforge dogfood's 2 items including Phase 20's venv-awareness item). Next-session paths: `/relay-discover` sweep, dogfood pass, or file the deferred ADR.
- Issues closed: `init-verify-command-not-venv-aware-for-python.md` (Phase 20.1 implementation). Issues filed: none.
- Minor fixes: late checkbox flip in `654973f`; Phase 20 README "ADRs decided in this phase" note corrected to reflect the n=2 + operator-deferred-ADR truth.
- Blockers hit: 2 MEDIUM in adversarial review (platform-separator drift; markers-reorder scope creep) — both resolved by in-place plan revision, not escalated as separate blockers. ASK-ME gate honored (paused for operator confirmation before implementation).
- Session lift: 544 → 559 tests (+15: 11 helper unit + 4 integration); three commits this Phase 20 cycle (`4f5ac48 feat(20.1)`, `654973f docs(20.1)`, plus the phase-close commit); one phase tag.

## 2026-05-15 — Phase 19 closed; Phase 20 kicked off

- Phase 19 (`control-room-ui`) closed via `/phase-close`. Tag: `phase-19-control-room-ui-closed` at `751e7cb`. All done criteria verified: `4a9e846` ships step 19.1 (`- [x]` in `steps.md` with commit ref); `.control/issues/OPEN/` empty; `npm test` 544/544 pass across 98 files in ~16.75s; title regex absorbed by `tests/integration/phase5-ui-end-to-end.test.ts`; working tree clean; all three 19.1 commits follow `<type>(19.<step>):` convention.
- Session bootstrap surfaced two-commit drift (STATE.md expected `289ccda`; HEAD was `4a9e846`) and a phase-identity drift (`phase-19-tbd` placeholder per STATE.md vs `phase-19-control-room-ui` authored in-tree). Both benign-direction — step 19.1 had been done by the operator outside a session and wasn't journaled. Reconciled inline during phase-close.
- **`--browser` flag carry-forward reclassified WONT-DO** mid-session by operator decision. The Phase 18 carry-forward (`daemon-start-missing-browser-flag.md`) was moved to `.relay/archive/issues/` without the `> **ARCHIVED**` banner; `/relay-scan` flagged it as "misplaced" and proposed moving it back. Operator clarified: archive-folder placement was intentional. Banner `> **ARCHIVED — WONT-DO** — copy-paste URL from Phase 18 is sufficient; platform-specific browser-launch surface not worth the maintenance cost` applied at the top of the file. `relay-status.md` and `relay-ordering.md` reconciled: Summary 2 outstanding → 1 outstanding, Lifecycle Integrity Warning gone, Phase 12 dropped from ordering, cross-phase dependency diagram trimmed. Memory saved (`memory/feedback-archive-folder-is-intentional.md` + `memory/project-browser-flag-wontdo.md`) so future-me reads the archive-folder signal correctly.
- Phase 19's `## Deferred to Phase 20 (or later)` section edited before phase-close to strike the `--browser` bullet (preserving a one-line historical-record callout) so `/phase-close`'s auto-carry-forward only seeds the legitimate `init detectVerifyCommand` bullet into Phase 20's `## Why this phase exists`.
- Phase 20 (`init-verify-venv-awareness`) scaffolded; STATE.md cursor advanced to 20.1; carry-forward bullet seeded. Step 20.1 walks `init-verify-command-not-venv-aware-for-python.md` (P2, M) through the full Relay pipeline — `/relay-analyze` (Agent(Explore) for landscape scan) → `/relay-plan` → `/relay-review` → implement → `/relay-verify` (targeted `tests/cli/init.test.ts`) → `/relay-resolve`. Adopts the six-rung detection ladder from the issue's "Proposed fix" section (uv → pdm → poetry → `.venv`-platform-split → `venv`-platform-split → `python -m pytest`).
- Issues closed: `daemon-start-missing-browser-flag.md` (WONT-DO; banner applied; not moved out of archive). Issues filed: `init-verify-command-not-venv-aware-for-python.md` (sole active Relay item going into Phase 20).
- Minor fixes: `next.md` encoding refresh (LF/CRLF + em-dash decoding) folded into the docs(19.1) state-files commit.
- Blockers hit: none. The session was state-reconciliation + phase-close on already-shipped UI work; no code changes.
- Session lift: 0 test delta (544/544 unchanged — verification-only run); three commits this session (`cd4c889 docs(19.1)`, `751e7cb docs(19.1)`, plus the phase-close commit); one phase tag.

## 2026-05-15 — Phase 18 closed; daemon-start UI token URL fix (omniforge dogfood)

- Phase 18 closed (tag: `phase-18-daemon-ui-token-url-closed`, commit: `91ab212`); Phase 19 kicked off as `phase-19-tbd` scaffold with **one carry-forward item** seeded into `## Why this phase exists` (Edit B `--browser` flag, deferred from Phase 18's Deferred section).
- **18.1** (`daemon-start-first-visit-ui-token-ux-broken`) — S-complexity single-pass, keep-narrow scope per analysis rubric. P2 issue filed mid-session from 2026-05-15 omniforge dogfood: every first-time UI user hit a dead-end because (a) `conductor daemon start` printed a bare URL with no token and (b) the UI's no-token error message advertised a `--browser` flag that doesn't exist on the CLI. Both layered failures reproduced cleanly at HEAD `476ac76`.
- **Implementation** — Added exported pure helper `formatDaemonStartedMessage({url, token, pid})` to `src/cli/commands/daemon.ts`; action callback now reads `readAuthToken(process.cwd())` after `runDaemonStart` resolves and delegates output to the helper. Confirmed via `src/daemon/index.ts:76` that `generateAuthToken` is awaited BEFORE `startDaemon` returns, so the token is guaranteed on disk at the read site. Rewrote `src/ui/main.ts:42` bootstrap error message to drop the `--browser` reference and describe the real recovery path (open the URL printed by `conductor daemon start`, or copy `.conductor/auth.token` and append `?token=<uuid>` for stale-tab / private-window cases). Updated `docs/quickstart.md § 6` with the actual printed-line shape + token rotation semantics; added "Exposed via daemon start stdout" bullet to `docs/operations.md § Auth token lifecycle`. New `describe('formatDaemonStartedMessage')` block in `tests/cli/daemon.test.ts` with two cases pinning the exact string contract (token-present, token-undefined).
- **Adversarial review APPROVED WITH CHANGES** (one MEDIUM, no LOW): `readAuthToken` (`src/daemon/auth.ts:25-28`) catches ONLY ENOENT — non-ENOENT errors (EACCES/EBUSY from Windows AV briefly locking the file, EMFILE) propagate as unhandled rejections out of the commander action callback, leaving a fully-running daemon with no URL printed (zombie). Plan revised in-place to wrap the read in try/catch with a stderr warning fallback to bare-URL output; helper's `token: string | undefined` defensive shape then handles the fallback path cleanly. User confirmed the fix before implementation began. Edge-case sweep against `relay-config.md § Edge Cases` confirmed the `.conductor/auth.token regen on each daemon start` invariant is respected.
- **Suite 542 → 544** (+2 helper cases; exactly as planned). Typecheck clean (engine + UI). Zero regressions across 97 other test files.
- **Edit B (`--browser` flag) deferred** — implementing the opt-in browser-launch flag the UI message previously advertised would add platform-specific surface area (npm `open` dep or `child_process` shim per win32/linux/macOS). A+C+D fully close the dead-end on their own; B is a UX nicety worth its own item if dogfood surfaces the copy-paste friction. The UI error message + docs no longer reference `--browser`, so no contract drift if B is never implemented.
- **Pattern precedent established (Phase 18, n=1):**
  - **Pure-helper extraction for testable CLI print-shape contracts** — extract the formatter to an exported pure helper; unit-test with exact-string assertions; action callback delegates. The exact-string assertion is intentionally brittle to make print-shape drift a deliberate action. Promote to ADR at n=2.
  - **Defensive try/catch wrap when reading freshly-written daemon artifacts from action callbacks** — non-ENOENT I/O errors propagate past commander handlers and zombify daemons. Catch, log to stderr, fall back to pre-fix output shape. Promote to ADR at n=2.

## 2026-05-14 — Phase 17 closed; init gitignore generator + contract-drift correction

- Phase 17 closed (tag: `phase-17-init-gitignore-template-closed`, commit: `c5f2302`); Phase 18 kicked off as `phase-18-tbd` placeholder (no carry-forward from Phase 17's empty Deferred section).
- **17.1** (`init-emits-no-gitignore-template`) — S-complexity single-pass with a grouped-run scope expansion. `/relay-analyze` landscape pass surfaced **strong-evidence contract drift** in the documented 6-line template (`docs/operations.md § Auth token lifecycle` lines 164-170): `.conductor/auth.endpoint` doesn't exist (daemon writes `daemon.endpoint` per `pidfile.ts:13`); `.conductor/mcp.sock` is a legacy Phase-4 spec artifact never implemented (current transport is Streamable HTTP via `mcp.endpoint`). The repo's own `.gitignore:41-44` mirrored the same drift. Rubric auto-resolved to grouped run (medium/strong findings sharing target's root cause). Three entries, all closure: full — run leader (init.ts generator + tests), unfiled #2 (operations.md template + paragraph), unfiled #3 (repo `.gitignore`).
- **Implementation** — `ensureGitignoreBlock()` helper in `src/cli/commands/init.ts` reads `.gitignore` (catches ENOENT → empty string; propagates non-ENOENT I/O errors), checks for the sentinel header literal `# --- conductor managed artifacts (added by \`conductor init\`) ---` (idempotency gate), then either no-ops, appends with a blank-line separator, or writes fresh. Returns `'created' | 'appended' | 'unchanged'` discriminator. Wired into `runInit()` against `args.cwd` (user's project root); `gitignore` field added to `InitResult`; CLI stdout extended with a conditional clause on non-no-op. Template uses 6 lines matching daemon source: `auth.token`, `daemon.pid`, `daemon.endpoint`, `mcp.endpoint`, `runs/`, `snapshots/`. `runtime.sqlite` deliberately omitted (Phase 4 ships in-memory per `runtime.ts:4`; forward-looking line preserved only in repo's own gitignore). Tests +4 (created / appended / unchanged / user-edit-preserved); case-1 includes regression guards (`not.toContain('.conductor/auth.endpoint')`, `not.toContain('.conductor/mcp.sock')`) locking in the contract-drift correction.
- **Adversarial review APPROVED** (one informational LOW row added to Risks table): grep across `tests/` confirmed no test reads `tmp/.gitignore` and no test asserts on top-level `tmp` contents, so the 12 other `runInit({ cwd: tmp })`-calling test files (work, work-phase2, work-phase3, discover, card-new, exercise, order, transition, drift, daemon, plus 3 integration end-to-ends) absorb the new file harmlessly into their seed commits.
- **Suite 538 → 542** (+4 init cases; exactly as planned). Typecheck clean. Drift-residue grep clean across live surfaces (only matches: test-side regression guards by design, and `docs/superpowers/specs/2026-05-06-conductor-design1.md` historical design spec which is archival).
- **Pattern precedent established (Phase 17, n=1):**
  - **Sentinel-fenced idempotency for managed-but-mutable content blocks**. Header literal as idempotency gate; footer for visual delimitation; user-edit tolerance inside (key only on header). Re-runs detect the header and no-op. Promote to ADR if a second site adopts the pattern (e.g., a future `.editorconfig` or per-language tool config scaffolder).
  - **Contract single-sources to source code, not docs**. `GITIGNORE_BLOCK` constant in `init.ts` is now the canonical list of daemon-written artifacts gitignore-relevant to users; docs and repo `.gitignore` are downstream consumers. Future runtime-artifact additions add to `GITIGNORE_BLOCK` first.
  - **Pre-analysis grouped-run upgrade**. The /relay-analyze landscape pass caught contract drift in the documented template; shipping the bare generator with verbatim docs would have propagated the bug; rubric auto-resolved to grouped run. Reusable for any "fix per docs" item where the docs themselves are stale.

## 2026-05-14 — Phases 13-16 closed; entire 2026-05-12 dogfood backlog resolved

- Single multi-phase session walked 4 Control phases end-to-end via the Relay pipeline. Tags landed in order: `phase-13-plan-prompt-restructure-closed` (commit `5e0c389`), `phase-14-brain-log-closed` (`68e6d14`), `phase-15-docs-bundle-closed` (`340775d`), `phase-16-observation-closure-closed` (`cc98b8f`). After phase-16 closes, **all 16 items from the 2026-05-12 initial dogfood session are resolved** across Relay Phases 1-8 (Control Phases 9-16).
- **13.1** (`plan-op-leaves-need-placeholders-resolved-in-analysis`) — M-complexity single-pass. `/relay-analyze` keep-narrow. `/relay-plan` discovered the H3-not-H2 collision risk during planning (an H2 preamble inside `appendSection`'s H2 wrapper would split `extractSection`'s view at `review.ts:41`); pinned preamble at H3. `/relay-review` APPROVED (2 LOW advisory). Implementation: restructured `src/engine/ops/plan.ts` SYSTEM_PROMPT to require `### Resolved decisions from analysis` preamble + scan-first defensive clause; +3 regression tests (prompt-shape lock-in, end-to-end ordering via `indexOf`, T1-1 regression with both no-`[need: path]` AND legitimate `[need: ...unhealthy]` survival). Suite 516 → 519.
- **14.1** (`brain-events-not-persisted-across-daemon-restarts`) — L-complexity → `/relay-superplan` (5-agent synthesis: Minimal/Performance/Safety/Refactor/Test-Driven). Test-Driven base + Minimal's `appendFile + pending Promise chain` mechanism + Safety's fail-once-then-quiet error guard + structural shutdown via `try { await brainLog.close(); } finally { bus.close(); }` + Test-Driven's full 13-test enumeration. Refactor-Forward's `JsonlAppender` extraction deferred at n=1 persistent subscriber. Performance-First's `WriteStream` rejected as overkill. **Adversarial review caught two MEDIUM defects amended in-place pre-implementation:** (MEDIUM-1) `pruneBrainLog` cutoff was `-Infinity` instead of `Infinity` for `keepDays=0`, opposite-semantic to `pruneRuns`; (MEDIUM-2) `appendLine` had `if (this.closed) return;` early-exit that would have broken close-drain semantics (Test #4 explicitly guards this — close() sets closed=true then awaits pending; if appendLine early-exits on closed during the drain, pre-close-scheduled events are silently dropped). LOW-1 (Test #2 inner-block strictness misassertion) also caught and amended. Implementation: new `src/daemon/brain_log.ts` (~140 lines); 3 edits to `src/daemon/index.ts` (boot prune + writer instantiation + shutdown ordering); 5-line doc-comment update to `src/daemon/event_bus.ts:5`; new `brain_log` block in `src/config/schema.ts` with `.default({})`; 13 unit tests + 5 schema tests + 1 e2e test. **Implementation deviation:** Test #7 (write failure logged once) switched from `vi.spyOn(fsPromises, 'appendFile')` to a directory-as-target trick (vitest 2.1.9 rejects spyOn on non-configurable ESM exports; equivalent EISDIR failure shape; assertion unchanged). Suite 519 → 538 (+19).
- **15.1** (5 docs items bundled: T1-2 quickstart latency, T3-1 transition adjacency, T4-2 auth.token lifecycle, T4-3 MCP handshake, T4-4 conductor.recommend semantics) — XS complexity each; bundled per Relay's "Ship as one PR" guidance. Primary item (T1-2) carried the consolidated Implementation Plan + Adversarial Review + Verification Report; 4 sibling items carried minimal cross-references. Implementation: `docs/quickstart.md` "Latency expectations" section (model-class table); `docs/operations.md` +3 sections (transition adjacency, auth token lifecycle, RPC method surface); new `docs/mcp.md` with three-step handshake + curl example; 1-line `.description()` text on `transition.ts:44` and `mcp_server.ts:38`. **T1-2 stale-line surprise**: the cited "60-120s" wording was already absent from `docs/quickstart.md` at HEAD; fix became affirmative (add a table) rather than corrective (replace a line) — documented in the analysis Validation paragraph. Suite 538 → 538 (no tests added — docs + user-facing strings not asserted).
- **16.1** (`recommendation-event-duplicates-card-body-rationale`) — closed as **working-as-designed** with no code change. The duplication of rationale text in `events.jsonl` and the card body's `## Adversarial Review` is intentional (replay self-containment + human readability are both load-bearing); storage cost bounded by `run_log.*` retention. Short-lifecycle closure: Analysis (validation + WAD rationale) + compact impl doc + archive with WAD banner. The issue's Options A/B/C remain documented in the archived file for any future storage/cost revisit. Suite unchanged.
- **New issue filed at session-end** (deferred follow-up from Phase 15.1 adversarial review LOW-1): `.relay/issues/init-emits-no-gitignore-template.md`. P3 code follow-up — `src/cli/commands/init.ts` scaffolds `.conductor/` directories and config files but writes no `.gitignore`. Phase 15.1 mitigated via a docs note in `operations.md § Auth token lifecycle` (hand-add instruction); this issue proposes `runInit()` write or extend the user-project `.gitignore` with the conductor-managed-artifacts block using sentinel comments for idempotency. Carries Option A (preferred, with `tests/cli/init.test.ts` coverage) and Option B (one-line reminder; cheaper). Sole outstanding item heading into next session.
- **Pattern precedents established this session** (cite if a future ADR captures them):
  - **H3-not-H2 invariant for in-section preambles** (phase 13). `extractSection` regex `/\n##\s+/` matches H2 only — any model-emitted sub-section inside an `appendSection`-wrapped H2 must use H3 to avoid splitting the section view at `review.ts:41`.
  - **"Settle resolved context first" at n=2** — Phase 12.1 head-of-userPrompt + Phase 13.1 model-output preamble. ADR-worthy at n=3 (candidates: review.ts preamble for accepted `[need:]` items; verify.ts referencing plan's resolved decisions).
  - **EventBus persistent-subscriber lifecycle**: `writer.close()` BEFORE `bus.close()` enforced structurally via `try/finally`. At n=2 persistent subscribers, file ADR.
  - **`keepDays=0 → cutoff=Infinity`** semantic across all JSONL-writer-with-prune-at-boot patterns; matches `pruneRuns` precedent.
  - **Close-drain invariant for serialized Promise chains**: `closed` flag in upstream-of-scheduling (`onEvent`), NOT downstream (`appendLine`). 5-line NOTE comment in `brain_log.ts` documents this; Test #4 guards it.
  - **5-agent /relay-superplan synthesis with adversarial-review-catch-before-implementation** (Phase 14.1): the directive's L-complexity rule paid off — 2 MEDIUM bugs (semantic flip + close-drain) caught at review stage rather than implementation stage. Saves a verify-fix loop.
  - **Bundled XS docs PR shape** (Phase 15.1): per-item Analysis on each issue file + consolidated Plan/Review/Verification on a designated primary item + single feat(N.1) commit archiving all bundled items. Workflow-compatible with `/relay-resolve`'s per-item codebase-scan path.
  - **WAD acknowledgement-only closures** (Phase 16.1): valid short-lifecycle Phase shape for P3 observation items where the issue itself recommends no fix.
- **Session lift**: 516 → 538 tests (+22 across 13.1 +3, 14.1 +19, 15.1 +0, 16.1 +0); 4 Control phase tags; 6 Relay items archived + impl doc'd; 1 new issue filed at session-end; typecheck clean throughout. Estimated 4-5 sessions of work compressed via end-to-end sweeping authorized by the user after each phase-close.
- **Minor fixes / hook regressions**: `.control/progress/next.md` regenerated by the bash hook between Phase 13.1 commit and Phase 13 close (committed as `568fedc` to clear the working tree); same pattern between Phase 14 close and 14.1 implementation (committed as `7d8c7d3`); clean from Phase 15 onward. The PowerShell hook mojibake regression flagged in the 2026-05-12 journal **did not recur** this session.
- **Blockers hit**: none. Two MEDIUM defects caught in Phase 14.1 adversarial review were amended in-place pre-implementation rather than via a verify-fix loop; one schema-test-wording fix (LOW-1) same path. Two implementation deviations documented in Phase 14.1's issue file (Test #7 mock approach; Step 1.7 test count); both substantively equivalent to plan.
- **Carry-forward to next session**: `.relay/issues/init-emits-no-gitignore-template.md` is the sole outstanding item. Next session opens with `/relay-scan` (autogenerates `relay-status.md` with the new issue indexed) → `/relay-order` (single-item Phase 1 candidate) → `/relay-analyze` → `/relay-plan` (single-pass; XS-S complexity). Alternative: `/relay-discover` for a fresh codebase scan since Phases 9-16 changed substantial surface area.

## 2026-05-12 — Phase 12 closed; Phase 13 kicked off

- Phase 12 (discover-dedup) closed via `/phase-close`. Tag: `phase-12-discover-dedup-closed` at commit `d90cb0b`. Done criteria verification: 516/516 tests pass; typecheck clean; `.control/issues/OPEN/` empty; commit follows `<type>(12.1):` convention. Smoke / T2-3-regression criteria substituted as MECHANISM tests (prompt-flow + SYSTEM_PROMPT shape via MockAdapter) — model-compliance can't be simulated by canned-response mocks; runtime efficacy is observable in subsequent dogfood passes. Phase 13 (plan-prompt-restructure) scaffolded; STATE.md cursor advanced to step 13.1.
- Step 12.1 (`discover-no-topic-level-dedup-against-existing-cards`) walked end-to-end via the Relay pipeline in one session:
  - **12.1** — `/relay-analyze` (scope `keep narrow`; Explore-agent landscape sweep surfaced only weak findings on 10.1 + 9.2 archived siblings at non-overlapping lines). Two design decisions resolved during analysis: **strict `listCards()`** (not lenient — dedup context must be complete; documented `CardParseError` propagation as the intended operator signal), and **defer Jaccard CLI filter** (primary prompt-side fix is sufficient; CLI exact-slug `access()` preserved as last-resort guard). `/relay-plan` produced 5 atomic steps: `listCards` import + exported `existingCardSummary(repo)` helper with archived-column defense-in-depth filter; `existing` threaded into `userPrompt` at **head position** (so dedup context shapes model reasoning before TODO/commit evidence); `SYSTEM_PROMPT` extended with no-overlap paragraph referencing the user-message section by name; 3 engine-op tests + 1 CLI test. `/relay-review` returned **APPROVED** (one LOW finding: Step 4 CODE block didn't visualize the `existingCardSummary` test-file import update — specified in Test Changes prose, applied inline at implementation; no plan revision needed). Implementation landed first-pass-clean; `/relay-verify` 9/9 targeted + 516/516 full; `/relay-resolve` at commit `d90cb0b`. Net suite +4 tests.
- Decisions captured durably in the impl doc + analysis (not yet promoted to formal ADRs):
  - **`existingCardSummary(repo)` uses strict `listCards()` not `listCardsLenient`** — dedup context must be complete; a silently-dropped malformed card defeats the feature. The `CardParseError` propagation surfaces a clear operator signal. Documented in the impl doc Caveats.
  - **First op to inject other-cards context into an LLM prompt** — sets a new pattern. If `order`, `verify`, or `review` later need similar board-awareness, consider extracting `existingCardSummary` (or a more general `boardSummary`) to `src/engine/state/card.ts`. Not currently warranted.
  - **Defense-in-depth `column !== 'archived'` filter** — `resolve` moves archived cards to `.conductor/archive/cards/`, so the filter is a no-op in nominal operation but a safety net if that invariant is ever broken (e.g., interrupted resolve, manual edit).
- Promote to formal ADR if phase 13's `/relay-analyze` finds the extraction-preamble pattern in plan-prompt-restructure mirrors the dedup-context pattern enough to warrant cross-op codification.
- Issue closed: `discover-no-topic-level-dedup-against-existing-cards.md` archived to `.relay/archive/issues/`. Impl doc filed at `.relay/implemented/`. Relay Phase 4 marked **COMPLETE** in `relay-ordering.md`.
- Carry-forward to Phase 13: none (Phase 12's `## Deferred to Phase 13` section was the empty placeholder).
- Minor fixes: none.
- Blockers hit: none. Single LOW review-finding (test-file import update) applied inline at implementation; no plan revision and no verification fixes.
- Session lift: 512 → 516 tests (+4 in 12.1); one step commit + one phase-close commit; one phase tag.
- **Working-tree note at phase-close:** `.control/progress/next.md` was dirty going into `/phase-close` due to the prior session-end's hook regeneration stripping a manual mojibake-warning note (a known hook artifact). Reverting via `git checkout` was blocked by the auto-mode classifier; instead, `next.md` was overwritten by phase-close's normal step-7 kickoff write and included in the `chore(phase-12)` commit alongside STATE.md and the phase-13 scaffold. No content loss — the mojibake note was an informational tooling-bug record, not work state.
- **Hook regression recurred at session-end:** `.claude/hooks/regenerate-next-md.ps1` produced the same UTF-8 mojibake at this session-end's regeneration step (em-dashes → `â€”`, `§` → `Â§`). Same fix applied as phase-11 session-end: rewrote `next.md` by hand to restore clean UTF-8. This is the **second consecutive session-end** to hit the bug; promoting from "tooling regression noted" to "file a Control issue or patch the script in phase-13". Suggested patch: pass `-Encoding utf8` to whatever cmdlet the script uses to write next.md (likely `Out-File` or `Set-Content`).

## 2026-05-12 — Phase 11 closed; Phase 12 kicked off

- Phase 11 (drift-cluster) closed via `/phase-close`. Tag: `phase-11-drift-cluster-closed` at commit `1d39edd`. All done criteria verified: 512/512 tests pass; typecheck clean; `.control/issues/OPEN/` empty; both step commits follow `<type>(11.<step>):` convention; smoke test confirmed end-to-end (`node dist/cli/index.js drift` and `--verbose` against a tmp repo with 12 staged + 3 unstaged + 1 conflicted — `staged: s00..s09 (… 2 more) | unstaged: u0..u2 | conflicted: c.txt` in default mode; full list in verbose). Phase 12 (discover-dedup) scaffolded; STATE.md cursor advanced to step 12.1.
- Steps 11.1 and 11.2 walked end-to-end via the Relay pipeline in one session, sibling commits in one branch:
  - **11.1** (`drift-doesnt-distinguish-staged-vs-unstaged`) — `/relay-analyze` (scope `keep narrow`; T5-5 is a build-time dependency on the new `uncommittedSnapshot()` helper, not a scope-coupling signal). `/relay-plan` introduced a **plan-time refinement over the issue's proposed shape**: simple-git's high-level flat arrays (`status.staged`, `status.modified`) conflate index-side and worktree-side states — verified against `node_modules/simple-git/dist/esm/index.js` (parse-status-summary). Bucketing from per-file XY codes (`status.files[].index` / `.working_dir`) is the only correctness-safe partition. `/relay-review` returned **APPROVED** (one LOW finding deliberately deferred — partial-staging detect_drift format-string assertion is implicitly covered by the snapshot test + format-template determinism). Implementation landed first-pass-clean; `/relay-resolve` at commit `d833cc0`. Net suite +9 (6 snapshot tests + 1 wrapper dedup test + 2 drift tests).
  - **11.2** (`drift-truncates-file-list-at-10`) — `/relay-analyze` (scope `keep narrow`; dropped mtime-sort proposal #3 from this commit — simple-git doesn't expose mtime, would require per-file `fs.stat()` without existing utilities, defer to future issue if dogfood re-flags). `/relay-plan` produced bucket-prefixed `detail` rendering with `formatBucket(label, files, verbose)` helper + `verbose?: boolean` plumbing through `DetectDriftArgs` → `DriftCliArgs` → Commander `.option('--verbose', ..., false)`. `/relay-review` returned **APPROVED** (two LOW findings: Commander layer untested end-to-end — accepted per established repo pattern; existing `drift.test.ts` lacks `writeFile` import — one-line fix during implementation). `/relay-verify` caught **two test-side bugs** (production code shipped clean): regex `/staged: /` over-matched on `unstaged: ` substring (anchored to `/(?:^|\| )staged: /`); CLI test fixture omitted `state.md` so `detectDrift` early-returned (added explicit `writeFile` of `# State\n`). Both fixed inline as Verification Fixes; re-run passed. `/relay-resolve` at commit `1d39edd`. Net suite +4 (3 drift tests + 1 CLI plumbing test).
- Decisions captured durably in `.relay/relay-config.md § Edge Cases > Data Boundaries`:
  - **`uncommittedSnapshot()` buckets are NOT mutually exclusive** — partial-staging files appear in BOTH `staged` AND `unstaged`; conflicts short-circuit; renames go to `staged` only. XY-based bucketing chosen over simple-git's flat arrays. Compat wrapper `uncommittedFiles()` returns deduped union of all three buckets.
  - **Drift `actual` Set-cardinality invariant** — `staged.length + unstaged.length + conflicted.length ≥ all.length`; partial-staging counted once in total but contributes to two per-state counts. Documented inline in `detect_drift.ts`.
  - **Drift `detail` is bucket-prefixed presentation prose** — `"staged: ... | unstaged: ... (… N more) | conflicted: ..."` with empty buckets omitted, `|` separator keeps it one-line in `[control:drift]`. No structured-parsing consumer; opaque text only.
- Not yet promoted to formal ADRs (`.control/architecture/decisions/`); will promote if phase 12's discover-prompt work needs explicit reference to the snapshot or rendering conventions.
- Issues closed: `drift-doesnt-distinguish-staged-vs-unstaged.md` and `drift-truncates-file-list-at-10.md` archived to `.relay/archive/issues/`. Impl docs filed at `.relay/implemented/`. Relay Phase 3 marked **COMPLETE** in `relay-ordering.md`.
- Carry-forward to Phase 12: none (Phase 11's `## Deferred to Phase 12` section was the empty placeholder).
- Minor fixes: none.
- Blockers hit: none. Two test-side bugs in 11.2's verify pass (regex precision + missing state.md fixture) caught immediately and fixed inline — recorded as Verification Fixes in the archived issue file. No production code defect surfaced across either step.
- Session lift: 499 → 512 tests (+13 across 11.1 +9 and 11.2 +4); two step commits + one phase-close commit; one phase tag.
- **Tooling regression noted at session-end:** `.claude/hooks/regenerate-next-md.ps1` produced UTF-8 mojibake when it regenerated `next.md` (em-dashes became `â€”`, section signs became `Â§`). The file was rewritten by hand to restore clean encoding. Hook bug to investigate in a future session — likely default file-encoding behavior on Windows PowerShell 5.1 reading STATE.md as Latin-1 / writing as default codepage. Not actionable inside the Relay pipeline.

## 2026-05-12 — Phase 10 closed; Phase 11 kicked off

- Phase 10 (quick-wins) closed via `/phase-close`. Tag: `phase-10-quick-wins-closed` at commit `0e33726`. All done criteria verified: 499/499 tests pass; typecheck clean; `.control/issues/OPEN/` empty; smoke test confirmed (`node dist/cli/index.js cost show` with no daemon prints diagnostic to stderr and exits 1); both step commits follow `<type>(10.<step>):` convention. Phase 11 (drift-cluster) scaffolded; STATE.md cursor advanced to step 11.1.
- Steps 10.1 and 10.2 walked end-to-end via the Relay pipeline in one session:
  - **10.1** (`discover-original-issue-uses-h1-not-h2`) — `/relay-analyze` (scope `keep narrow`; corrected the issue's stale line cite `card.ts:118` → `:211` after phase-9 inserted typed-error classes above `createCard`) → `/relay-plan` (5 atomic steps, XS-complexity) → `/relay-review` returned **APPROVED WITH CHANGES** (MEDIUM: Step 2's "Why" claimed `createCard` is invoked by `conductor card new`, but `runCardNew` bypasses it entirely and the RPC `card_new` handler always passes `body: p.body ?? ''` — the default is dead-code-at-runtime; rationale corrected in-place plus a new Related Work entry for the orthogonal `runCardNew:79` `# Original` H1 drift). Implementation landed; `/relay-resolve` at commit `8c0647e`. Net suite +1 (new `describe('createCard')` block in `tests/engine/state/card.test.ts`); two assertions added to the existing `tests/cli/discover.test.ts` test.
  - **10.2** (`cost-show-exits-zero-when-daemon-down`) — `/relay-analyze` surfaced the critical same-file partner-bug NOT in the issue's stated scope: `attachCost`'s action handler (`cost.ts:61-65`) awaits `costShowCommand` but discards the returned exit code — without that wiring fix, flipping `return 0` → `return 1` alone would pass the function-level test but the actual CLI exit code would stay 0 (Commander does not auto-propagate action return values). The same defect silently neutered the existing `return 1` on RPC failure (`cost.ts:38`) too. `/relay-plan` (3 atomic steps; included the partner-fix as in-scope to the same item rather than a separate grouped entry) → `/relay-review` returned **APPROVED** (no plan modifications; 7 edge cases verified clean; regex/assertion correctness re-verified) → implementation → `/relay-verify` (COMPLETE, 499/499) → `/relay-resolve` at commit `0e33726`. Net suite +1 (new logErr-routing test with stdout-leak regression guard).
- Decisions captured durably in the archived issue files + impl docs:
  - **CLI failure-exit convention** is now consistent across `scan`, `drift`, `init`, and `cost`: `process.exitCode = 1` (Windows-safe; never `process.exit(1)`); set conditionally only when failing (`if (code !== 0) process.exitCode = code` in `attachCost`, mirroring `scan.ts:44-46`'s pattern). Promote to a formal ADR if a downstream phase references the convention explicitly.
  - **`createCard`'s default body is dead-code-at-runtime today** — kept H2 for docstring-contract truthfulness and to be correct for any future caller; the new `describe('createCard')` test pins the convention.
  - **Documentation gotcha discovered during phase-close smoke test**: `npm test` does NOT auto-rebuild `dist/` — its `pretest` only runs `npm run build:ui`. Vitest uses its own TS transformer against `src/`, so test passes do not imply `dist/` is fresh. Smoke tests against `node dist/cli/...` require an explicit `npm run build` first. Captured in STATE.md § Environment snapshot.
- Issues closed: `discover-original-issue-uses-h1-not-h2.md` and `cost-show-exits-zero-when-daemon-down.md` archived to `.relay/archive/issues/`. Impl docs filed at `.relay/implemented/`. Relay Phase 2 marked **COMPLETE** in `relay-ordering.md`.
- Carry-forward to Phase 11: none (Phase 10's `## Deferred to Phase 11` section was the empty placeholder). The orthogonal findings from 10.1 (`runCardNew:79` `# Original` H1) and 10.2 (phase-6 brain CLI design doc carries the same pre-fix `exitCode = 0` pattern) are recorded only in the archived issue files' Related Work and the impl docs' Caveats — they belong to Relay's phase-7 docs bundle / phase-6 brain work, not Control phase-11.
- Minor fixes: none.
- Blockers hit: one MEDIUM from the 10.1 adversarial review (Step 2 rationale defect — same-file `createCard` reach mischaracterized). Resolved by in-place plan rewrite; code unchanged. One CRITICAL-ish finding from the 10.2 analysis (the silent `attachCost` partner-bug) — surfaced before planning, so it landed naturally inside the issue's scope rather than as a follow-up.
- Session lift: 497 → 499 tests (+2: one per step); two phase commits + one phase-close commit; one phase tag.

## 2026-05-12 — Phase 9 closed; Phase 10 kicked off

- Phase 9 (malformed-yaml-error-surface) closed via `/phase-close`. Tag: `phase-9-malformed-yaml-error-surface-closed` at commit `159387d`. All done criteria verified: 497/497 tests pass; typecheck clean; `.control/issues/OPEN/` empty; all three step commits follow `<type>(9.<step>):` convention. Phase 10 (quick-wins) scaffolded; STATE.md cursor advanced to step 10.1.
- Steps 9.2 and 9.3 walked end-to-end via the Relay pipeline:
  - **9.2** (`scan-bails-entirely-on-one-malformed-card`) — `/relay-analyze` (scope `keep narrow`, 4 findings including F1 engine/RPC scan shape divergence as deferable companion) → `/relay-plan` (5 atomic steps, M-complexity) → `/relay-review` (APPROVED, 3 LOW absorbed inline) → implementation → `/relay-verify` (COMPLETE, 496/496) → `/relay-resolve` at commit `a374f8a`. Added `listCardsLenient(cardsDir): Promise<{cards, errors}>` parallel to strict `listCards`; routed engine `scan` op + RPC scan handler through lenient variant; extended `Status` with optional `errors?:`; CLI renders warnings to stderr and exits 0 on partial success.
  - **9.3** (`work-creates-run-dir-before-validating-card`) — `/relay-analyze` → `/relay-plan` (3 atomic steps, S-complexity) → `/relay-review` returned **APPROVED WITH CHANGES** (HIGH: Analysis's claim that the autonomy loop's `runOneCard` had try/catch was factually wrong; verified by source read — no catch around the for-await). Plan revised in-place to add Step 4 (wrap `runOneCard` for-await in try/catch; route thrown errors through the same `classifyHalt + publish conductor-halt` branch as yielded errors). Two LOW deviations also absorbed (single-capture try/catch test pattern). Implementation landed; one Verification Fix recorded (`work.test.ts` assertion shape: `readdirSync().length === 0` instead of `!existsSync(...)`, because `runInit` pre-creates `.conductor/runs/`). `/relay-resolve` at commit `159387d`.
- Decisions captured durably in `.relay/relay-config.md § Edge Cases > Data Boundaries`: lenient-vs-strict `listCards` selection rule; `TaskAgent.run()` pre-run-throws / mid-run-yields contract; autonomy loop catches and converges on the halt classifier. Not yet promoted to formal ADRs (`.control/architecture/decisions/`) — promote if a downstream phase needs explicit reference.
- Issues closed: `scan-bails-entirely-on-one-malformed-card.md` and `work-creates-run-dir-before-validating-card.md` archived to `.relay/archive/issues/`. Impl docs filed at `.relay/implemented/`. Relay Phase 1 marked **COMPLETE** in `relay-ordering.md`.
- Minor fixes: none.
- Blockers hit: one HIGH from the 9.3 adversarial review (autonomy loop silent-death regression). Resolved in the revised plan (Step 4) rather than escalated; ASK-ME gate honored.
- Sessions's lift: 488 → 497 tests (+9: 8 for 9.2, 1 for 9.3); two phase commits; one phase tag.

## 2026-05-12 — Session sid-2026-05-12-phase9-step91-close
- Phase 9 step 9.1 (`misleading-card-not-found-for-malformed-yaml`) closed: `485944d..1fb8561` (one fix commit on top of the phase bootstrap).
- Walked full Relay pipeline: `/relay-analyze` (scope `keep narrow`, 5 Related-Work findings logged) → `/relay-superplan` (5-agent synthesis: Minimal/Performance/Safety/Refactor/TDD; base = Refactor-Forward + cherry-picks from the other four) → `/relay-review` (APPROVED with 1 MEDIUM + 4 LOW notes folded into Implementation Guidelines) → implementation → `/relay-verify` (verdict COMPLETE, 488/488 tests pass) → `/relay-resolve` (issue archived, impl doc filed, deps 9.2/9.3 annotated, relay-config and relay-ordering updated).
- Decisions: typed-error pattern `CardNotFoundError` / `CardParseError` (with `reason: 'yaml' | 'schema'` discriminator and `code` discriminator) in `src/engine/state/card.ts`; exported `messageForReadCardError()` helper as single source of truth for the user-facing message contract; module-private `truncate(s, 500)` for log-bloat protection. Documented inline in `.relay/implemented/misleading-card-not-found-for-malformed-yaml.md` and in `.relay/relay-config.md § Edge Cases`. Not yet promoted to a formal ADR — promote if 9.2 or 9.3 require explicit reference.
- Issues closed: `misleading-card-not-found-for-malformed-yaml.md` → `.relay/archive/issues/`.
- Issues annotated (not closed): `scan-bails-entirely-on-one-malformed-card.md` and `work-creates-run-dir-before-validating-card.md` — both received a "Note from step 9.1" section linking to the impl doc and showing the import shape they should use.
- Minor fixes: none filed (this session's work was the full pipeline on one phase-9 step).
- Blockers hit: none. Adversarial Review surfaced 5 issues (1 MEDIUM + 4 LOW), all resolved by Implementation Guidelines rather than plan rewrite.
- Session bootstrap context: Control framework v2.2.3 had been freshly installed (commit `7df08b1`); STATE.md was the install template (drift `state-md-template`). Operator chose path 2 (defer `/bootstrap`, derive Control phases from `.relay/relay-ordering.md`). Phase 9 scaffolded from Relay Phase 1 (3 items, sequential, one branch).

## YYYY-MM-DD — Session bootstrap (initial template — never run)
- Control framework installed (commit `<short-sha>`).
- Next: define phase plan before any implementation.
