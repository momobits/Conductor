# Next session kickoff

> Auto-generated by `/phase-close` (Phase 19 → Phase 20 transition) on
> 2026-05-15. The `.claude/hooks/regenerate-next-md.{sh,ps1}` hook will
> overwrite this on `/session-end`. Edit STATE.md's "Next action"
> or "Notes for next session" to influence this prompt; **do not edit
> next.md by hand** -- it's overwritten on every session end.

This is a Control-managed project. Bootstrap protocol:

1. Read `.control/progress/STATE.md` -- the single source of truth.
2. Read the current phase's `README.md` and `steps.md` (path in STATE.md).
3. Check `.control/issues/OPEN/` for current-phase blockers.

If the SessionStart hook is installed, steps 1-3 run automatically and you
see a structured `[control:state]` block instead of doing them by hand.

## Next action

**Drive `init-verify-command-not-venv-aware-for-python` (P2, M) through the full Relay pipeline as Phase 20 step 20.1.**

Issue: `.relay/issues/init-verify-command-not-venv-aware-for-python.md`. Sole active Relay item. The "Proposed fix" section lays out the six-rung detection ladder (uv → pdm → poetry → `.venv`-platform-split → `venv`-platform-split → `python -m pytest` fallback); the plan adopts that shape unless adversarial review surfaces a defect.

Pipeline:
1. `/relay-analyze` — dispatch `Agent(subagent_type=Explore)` for the broad scan; main-session reads only the issue spec + ≤5 directly-affected source files in full.
2. `/relay-plan` (M complexity, single-pass).
3. `/relay-review` — adversarial review. ASK ME if verdict is APPROVED-WITH-CHANGES or REJECTED.
4. Implement.
5. `/relay-verify` — `npx vitest run tests/cli/init.test.ts 2>&1 | Select-Object -Last 50`, then `npm test 2>&1 | Select-Object -Last 50`, then `npm run typecheck`.
6. `/relay-resolve` — commit `feat(20.1): detectVerifyCommand venv-aware for Python (uv/pdm/poetry/.venv/venv/python -m pytest)`.
7. `/phase-close` Phase 20 → tag `phase-20-init-verify-venv-awareness-closed`.

The Phase 18 carry-forward (`daemon --browser` flag) was closed WONT-DO mid-Phase-19 by operator decision; not in scope for Phase 20.

## Notes for next session

Phase 20 is a single-item Relay phase. Use `/relay-plan` single-pass (not `/relay-superplan` — reserve that for L items).

Context-preservation reminders:
- `/relay-analyze` — `Agent(Explore)` for the broad landscape scan; main session reads only the issue spec + ≤5 directly-affected source files.
- `/relay-verify` — targeted command piped through `Select-Object -Last 50`; if a verification-fix loop runs >3 iterations, dispatch `Agent(general-purpose)` for final diff + verdict.
- `/relay-resolve` — single-pass; commit at end.

When Phase 20 closes, active Relay backlog will be empty. Next-session paths revert to: `/relay-discover` codebase sweep, or dogfood pass.

Notebook step is skipped per `relay-config.md § Notebook Setup` (TypeScript-only project).
