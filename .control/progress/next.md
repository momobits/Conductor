# Next session kickoff

> Auto-generated by `/phase-close` (Phase 20 → Phase 21 transition) on
> 2026-05-15. The `.claude/hooks/regenerate-next-md.{sh,ps1}` hook will
> overwrite this on `/session-end`. Edit STATE.md's "Next action"
> or "Notes for next session" to influence this prompt; **do not edit
> next.md by hand** -- it's overwritten on every session end.

This is a Control-managed project. Bootstrap protocol:

1. Read `.control/progress/STATE.md` -- the single source of truth.
2. Read the current phase's `README.md` and `steps.md` (path in STATE.md).
3. Check `.control/issues/OPEN/` for current-phase blockers.

If the SessionStart hook is installed, steps 1-3 run automatically and you
see a structured `[control:state]` block instead of doing them by hand.

## Next action

**Phase 20 closed.** Active Relay backlog is empty — all 20 items closed across 11 Relay phases. Three recommended next-session paths to author Phase 21:

1. **`/relay-discover`** — codebase scan for new TODOs / drift / latent gaps surfaced since Phases 13-20 expanded engine, daemon, CLI, UI, and init surfaces. May produce zero or several new items.

2. **Dogfood pass** — run `conductor work <card>` on a real project. Phase 20's fix should validate that Python verify loops succeed on first run for projects with `.venv` / poetry / pdm / uv. Watch for other UI/CLI rough edges.

3. **File the deferred pure-helper-extraction ADR** — small discrete work-item. The pattern (Phase 18 `formatDaemonStartedMessage` + Phase 20 `detectPythonVerifyCommand`) reached n=2; STATE.md's promotion threshold fired; operator deferred filing during Phase 20 to keep that phase's scope narrow. Recommended path: `.control/architecture/decisions/0001-pure-helper-extraction-for-testable-cli-contracts.md` (verify next number against existing decisions), use `.control/templates/adr.md`, commit `docs(adr): ADR-0001 pure-helper extraction for testable CLI contracts`.

Phase 21's scaffold is a bare template at `.control/phases/phase-21-tbd/`. Its README and steps need authoring once the next item is chosen.

## Notes for next session

**Active Relay backlog is empty.** Path selection drives Phase 21:

- **/relay-discover** is the highest-leverage scan target if the operator wants the next item to be code-side; touches Phases 13-20's expanded surfaces (engine, daemon, CLI, UI, init).
- **Dogfood** has historically produced the highest-signal issues (Phase 18 + Phase 20 both originated from 2026-05-15 omniforge dogfood). If a real-project run surfaces friction, file via `/relay-new-issue` and pipeline through `/relay-analyze`.
- **ADR filing** is the cheapest path; produces a single `.control/architecture/decisions/000N-<slug>.md` file with no code change. Worth doing before n=3 hits — pattern is well-defined and the supporting examples are fresh.

Pattern precedents to reference if an ADR is filed:
- **Pure-helper extraction for testable CLI print-shape contracts** (n=2 — Phase 18 + Phase 20; ADR deferred).
- **Defensive try/catch wrap when reading freshly-written daemon artifacts from action callbacks** (Phase 18 — n=1).
- **Sentinel-fenced idempotency for managed-but-mutable content blocks** (Phase 17 — n=1).

Notebook step is skipped per `relay-config.md § Notebook Setup` (TypeScript-only project).
